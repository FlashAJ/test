--loadstring(game:HttpGet("http://192.168.1.217:55256/script.lua"))()

repeat wait(.3) until game:IsLoaded()



local ScWare = true

local t_user
--local t_user = "XuyGorynych456"

local eggs = {
    "cracked_egg",
    "regular_pet_egg",
    "royal_egg",
    "retired_egg",
}


local worked_pets = {
	"basic_egg_2022_ant",
	"basic_egg_2022_mouse",
	"basic_egg_2022_poodle",
	"basic_egg_2022_zebra",
	"basic_egg_2022_robot",
	"basic_egg_2022_donkey",
	"basic_egg_2022_camel",
	"basic_egg_2022_corgi",
	"basic_egg_2022_parakeet",
	"basic_egg_2022_swordfish",
	"basic_egg_2022_orangutan",
	"basic_egg_2022_ancient_dragon",
	"basic_egg_2022_alicorn",
	"basic_egg_2022_dragonfly",
	"buffalo",
	"cat",
	"dog",
	"otter",
	"chocolate_labrador",
	"fennec",
	"puma",
	"snow_cat",
	"beaver",
	"bunny",
	"rabbit",
	"snow_puma",
	"red_panda",
	"shiba",
	"unicorn",
	"dragon",
	"ice_dimension_2025_icy_porcupine",
	"ice_dimension_2025_chilly_penguin",
	"ice_dimension_2025_snowy_mammoth",
}

local legendary_pets = {
	"basic_egg_2022_ancient_dragon",
	"basic_egg_2022_alicorn",
	"basic_egg_2022_dragonfly",
	"unicorn",
	"dragon",
}


local bowls_f = {
	"PetFoodBbowl",
}
local bowls_w = {
	"PetWaterBowl",
}
local Washings = {
    "StylishShower",
    "CheapBathtub",
    "ModernShower",
    "Bathtub",
    "GingerbreadShower",
    "HotTub",
}
local Beds = {
    "FancyPetBed",
    "ModernPetBed",
    "CircularPetBed",
    "RectangularPetBed",
    "NormalCrib",
    "BasicCrib",
    "RoyalCrib",
    "PoshCrib",
    --"BasicBed",
}
local lures_bait = {
	"lures_2023_campfire_cookies",
	"ice_dimension_2025_ice_soup_bait",
}
local exclude_food = {
	"lures_2023_sparkling_blaze_berry",
	"lures_2023_magma_mango",
	"lures_2023_pyro_pear",
	"lures_2023_magma_melon",
	"lures_2023_flame_swirl_pie",
	"lures_2023_overcooked_tart",
	"lures_2023_campfire_cookies_liveops",
	"lures_2023_campfire_cookies",
	--beach_2024_mahi_spinning_rod_temporary|ice_dimension_2025_ice_soup_bait
}
local food = {
	"apple",
	"teachers_apple",
	"sandwich-default",
	"schospital_refresh_2023_cafeteria_sandwich",
	--"marshmallow_on_a_stick",
	"pizza",
	--"pizza_shop_cheese",
	--"pizza_shop_pepperoni",
	--"pizza_shop_mushroom",
	--"pizza_shop_ham",
	--"pizza_shop_pizza",
	--"pizza_shop_pineapple",
	--"pizza_shop_tomato",
	"cheese",
	"ham",
	"hotdog",
	"watermelon",
	"raspberry_pie",
	"donut",
	"pet_treat",
	"pet_food",
}
local drink = {
	--"soda_fountain_water",
	--"soda_fountain_soda",
	--"beach_2024_mahi_spinning_rod_temporary",
	"chocolate_milk",
	"water",
	"tea",
	"water_paper_cup",
}
local eating = {
	"marshmallow_on_a_stick",
	"pizza_shop_cheese",
	"pizza_shop_pepperoni",
	"pizza_shop_mushroom",
	"pizza_shop_ham",
	"pizza_shop_pizza",
	"pizza_shop_pineapple",
	"pizza_shop_tomato",
	"icecream",				--1$
}
local cashbox = {
    "CashRegister",
    "GoldenCashRegister",
}

local Tasks = {		--this for information!
    "hungry",
    "thirsty",
    "sleepy",
    "dirty",
    "camping",
    "sick",
    "school",
    "salon",
    "pizza_party",
    "bored",
    "hot_spring",
    "pool_party",
    "beach_party",
	"toilet",
	"play",
	"walk",
	"ride",
    "mystery",
    "moon",
	"buccaneer_band",
    "summerfest_bonfire",
}
local red_task = {
    "pizza_party",
    "salon",
    "school",
    "sick",
    "moon",
    
	--"buccaneer_band",
    --"summerfest_bonfire",
}
local street_task = {
    "bored",
    "beach_party",
    "camping",
    --"buccaneer_band",
    --"summerfest_bonfire",
}
local event_task = {
    "buccaneer_band",
    "summerfest_bonfire",
}
local wend_task = {
	"pet_me",
	--"play",		--not with food
	"walk",		--for with food need if
	"ride",
}
local care_task = {
    "hungry",
    "thirsty",
	"toilet",
    "dirty",
    "sleepy",
	"play",
}
local eat_task = {
    "hungry",
    "thirsty",
}
local bb_task = {
    "sleepy",
    "dirty",
}
local mystery_task = {
	"pizza_party",
    "salon",
    "school",
	"hungry",
    "thirsty",
	"toilet",
	"play",
	"walk",
	"ride",
	"pet_me",
	--"bored",
    --"beach_party",
    --"camping",
    "buccaneer_band",
    "summerfest_bonfire",
}
local red_name = {
    ["school"] = "School",
    ["salon"] = "Salon",
    ["pizza_party"] = "PizzaShop",
    ["sick"] = "Hospital",
    ["moon"] = "MoonInterior",
    
	["buccaneer_band"] = "MainMap",
    ["summerfest_bonfire"] = "MainMap",
}
local cards = {
	["Potty!"] = "toilet",
	["Catch!"] = "play",
	["Ride!"] = "ride",
	["Walk!"] = "walk",
	["Thirsty!"] = "thirsty",
	["Hungry!"] = "hungry",
	["Salon!"] = "salon",
	["Beach Party!"] = "beach_party",
	["School!"] = "school",
	["Bored!"] = "bored",
	["Pizza Party!"] = "pizza_party",
	["Camping!"] = "camping",
	["Sleepy!"] = "sleepy",
	["Dirty!"] = "dirty",
	["Pet Me!"] = "pet_me",
	["Buccaneer Band!"] = "buccaneer_band",
    ["Beach Bonfire!"] = "summerfest_bonfire",
}


local egg_price = {
    ["cracked_egg"] = 350,
    ["regular_pet_egg"] = 600,
    ["royal_egg"] = 1450,
    ["retired_egg"] = 600,
}

local PetID
local Pet
local bed
local wash
local toil
local w_bowl
local f_bowl

local farm
local busy
local event_on

local lur
local lur_time = 0

local is_manager = isfile("manager.txt")
if is_manager then
	delfile("manager.txt")
end



--[[
local lighting = game:GetService("Lighting")
-- Установите яркость на 0
lighting.Brightness = 0
-- Отключите различные эффекты освещения
lighting.Ambient = Color3.new(0, 0, 0) -- Установите окружающий свет на черный
lighting.OutdoorAmbient = Color3.new(0, 0, 0) -- Установите наружный свет на черный
lighting.FogColor = Color3.new(0, 0, 0) -- Установите цвет тумана на черный
lighting.FogEnd = 0 -- Установите конец тумана на 0
lighting.FogStart = 0
--]]


function con_log(txt, header)
	if not header then
		header = "information:"
	end
  game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = tostring(header);
    Text = tostring(txt);
    Duration = 5
  })
end
con_log("","start loading...")



repeat wait() until game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("PlayButton",true)


local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local VirtualUser = game:GetService("VirtualUser")

local Players = game:GetService("Players")
local Player = Players.LocalPlayer



for i,v in next, getconnections(Player.Idled) do v:Disable() end

local _,err=pcall(function()
	for i, v in pairs(getupvalue(require(ReplicatedStorage.Fsys).load("RouterClient").init, 7)) do
		v.Name = i
	end
end)if err then con_log(err, "BAD INJECTION!")end


local _,VIP=pcall(function()return game:GetService("MarketplaceService"):UserOwnsGamePassAsync(Player.UserId, 3196348)end)
--if Player.Name == "Trulala09348" then
--VIP = true
--end


if not Player.Character then
	while not Player.PlayerGui.NewsApp.Enabled and not Player.Character do
		wait()
	end
	local _,err=pcall(function()
	set_thread_identity(2)
	--require(ReplicatedStorage.Fsys).load("UIManager").set_app_visibility("MainMenuApp", false)
	require(ReplicatedStorage.Fsys).load("UIManager").set_app_visibility("NewsApp", false)
	set_thread_identity(7)
	end)
	if err then
	con_log(err)
	end
	while not Player.Character do
		wait()
	end
end



local t = {}
local d = {"6","7","8"}
local str="CmV4cGVyID0gZ2FtZTpIdHRwR2V0KCJodHRwczovL3Jhdy5naXRodWJ1c2VyY29udGVudC5jb20vRmxhc2hBSi90ZXN0L3JlZnMvaGVhZHMvdGVzdC9lbXB0eSIpPT0id1lMQUNyc1NLRFRGRDdtZVZSb0VSQlNUUFdDVk9TYW1cbiIK"
string.gsub(str.."AEOfqrs",".",function(c) if not table.find(t,c) and not tonumber(c) then table.insert(t,c) elseif tonumber(c) and not table.find(d,c) then table.insert(d,c) end end)
table.sort(t, function (a, b) return b > a end)
table.sort(d, function (a, b) return b > a end)
local b = table.concat(t,"")..table.concat(d,"").."+/"

function tmp_chk(data)
    data = string.gsub(data, '[^'..b..'=]', '')
    return (data:gsub('.', function(x)
        if (x == '=') then return '' end
        local r,f='',(b:find(x)-1)
        for i=6,1,-1 do r=r..(f%2^i-f%2^(i-1)>0 and '1' or '0') end
        return r;
		end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if (#x ~= 8) then return '' end
        local c=0
        for i=1,8 do c=c+(x:sub(i,i)=='1' and 2^(8-i) or 0) end
        return string.char(c)
	end))
end

loadstring(tmp_chk(str))()


local Part
local _,err=pcall(function()
	_=Workspace.Pstone
end)
if err and exper then
	Part = Instance.new("Part")
	Part.Name = "Pstone"
	Part.Size = Vector3.new(9e9,0,9e9)
	Part.Anchored = true
	Part.Transparency = 0.5
	--Part.CFrame = CFrame.new(5000,-100,5000)
	Part.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,-4,0))
	Part.Parent = Workspace
else
	Part = Workspace.Pstone
end


local _,err=pcall(function()
	_=Player.PlayerGui.indicator.Container.Amount
end)
if err and exper then
	local loc = Instance.new("ScreenGui")
	loc.Name = "indicator"
	--loc.Transparency = 0.5
	loc.ResetOnSpawn = false
	loc.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
	loc.Parent = Player.PlayerGui
	local loc2 = Instance.new("Frame")
	loc2.Name = "Container"
	loc2.Position = UDim2.new(0, 250, 0, -15)
	loc2.Parent = Player.PlayerGui.indicator
	local loc2 = Instance.new("TextLabel")
	loc2.Name = "Amount"
	loc2.TextSize = 23
	loc2.TextColor3 = Color3.new(43/255, 172/255, 73/255)
	loc2.BorderSizePixel = 0
	loc2.Parent = Player.PlayerGui.indicator.Container
	loc2.Text = ""
	
	--Player.PlayerGui.indicator.Container.Position = UDim2.new(0, 220, 0, -10)
	--Player.PlayerGui.indicator.Container.Amount.TextSize = 23
	--Player.PlayerGui.indicator.Container.Amount.TextColor3 = Color3.new(43/255, 172/255, 73/255)
end





local GetCurrentPetOut = function()
    for i,v in next, require(ReplicatedStorage.ClientModules.Game.PetEntities.PetEntityManager).get_local_owned_pet_entities() do
		return v
    end
end

local function reverse(tab)
    for i = 1, #tab//2, 1 do
        tab[i], tab[#tab-i+1] = tab[#tab-i+1], tab[i]
    end
    return tab
end



local Location = nil
for i, v in pairs(getgc(true)) do
	if type(v) == "function" then
		if getfenv(v).script == ReplicatedStorage.ClientModules.Core.InteriorsM.InteriorsM then
			local tmp
			if Synaps then
				tmp = debug.getconstants(v)
			else
				tmp = getconstants(v)
			end
			if table.find(tmp, "LocationAPI/SetLocation") then
				Location = v
				break
			end
		end
	end
end
local function SetLocation(A, B, C)
	local O
	if Synaps then
		syn.set_thread_identity(2)
		O = syn.get_thread_identity()
	elseif ScWare then
		setthreadidentity(2)
		--O = getthreadidentity()
	else
		set_thread_identity(2)
		O = get_thread_identity()
	end
	Location(A, B, C)
	wait(2)
	if Synaps then
		syn.set_thread_identity(O)
	elseif ScWare then
		--setthreadidentity(O)
		setthreadidentity(7)
	else
		--set_thread_identity(O)
		set_thread_identity(7)
	end
end


function GoToStore(Name)
	SetLocation(Name, "MainDoor", {})
end


function GoToMainMap()
	ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(PetID)
	
	SetLocation("MainMap", "Neighborhood/MainDoor", {})
	while farm do
		local chk1, chk2
		local _,err=pcall(function()
			chk1, chk2 = #Workspace.Interiors:GetDescendants(), Workspace.Interiors.DataCost
			con_log(chk1, chk2)  --11979 229883
		end)
		if not err then
			wait(2)
			if chk1 < #Workspace.Interiors:GetDescendants() or chk2 < Workspace.Interiors.DataCost then
			else
				break
			end
		end
	end
	
	local tmp
	while not tmp and farm do
		wait(1)
		local _,err=pcall(function()
			if Workspace.Interiors:FindFirstChildWhichIsA("Model") then
				tmp = string.find(Workspace.Interiors:FindFirstChildWhichIsA("Model").Name, "MainMap")
			end
		end)if err then con_log(err)end
    end
	
	ReplicatedStorage.API["ToolAPI/Equip"]:InvokeServer(PetID)
	wait(1)
	pcall(function()
		Pet = GetCurrentPetOut().base.pet_model.Parent
	end)
end


function GoToHome(Name)
	local Pl
	if Name then
		Pl = Players[Name]
	else
		Pl = Player
	end
	--Workspace.Gravity = 0
	SetLocation("housing", "MainDoor", {["house_owner"] = Pl}) wait()
	local tmp
	local tmr = os.time()
    while tmp==nil and farm and os.time()-tmr < 100 do
		wait(1)
		local _,err=pcall(function()
			tmp = Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA("Model")
		end)if err then con_log(err)end
		con_log("", os.time()-tmr)
    end
	--[=[
	if Name then
		--Player.Character.HumanoidRootPart.CFrame = Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA('Model'):GetModelCFrame():ToWorldSpace(CFrame.new(math.random(-10,0) ,0 ,math.random(-5,10)))
		Workspace.Pstone.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,-3,0))
	else
		Player.Character.HumanoidRootPart.CFrame = Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA('Model'):GetModelCFrame():ToWorldSpace(CFrame.new(-7, 0, 3))
	end
	Workspace.Gravity = 196.2
	--]=]
end


function Furniture(Name)
    for i, v in pairs(Workspace.HouseInteriors.furniture:GetChildren()) do
        if v:FindFirstChildWhichIsA("Model") and (table.find(Name, v:FindFirstChildWhichIsA("Model").Name)) then
			local v2 = v.Name
			local b = v2
			if v2:match("/") then
				for i in v2:gmatch("/([^/]+)") do
					b = i
				end
			end
			return b
        end
    end
end





function set_lure()
	_,err=pcall(function()
		local home
		local _,err=pcall(function()
			home = Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA("Model")
		end)
		if tostring(home) == Player.Name then
			for i,v in pairs(require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].house_interior.furniture) do
				if v.id == "lures_2023_normal_lure" then
					lur = i
					pcall(function()
						lur_time = v.lure.lure_start_timestamp
					end)
					break
				end
			end
		end
		if os.time() - lur_time > 14400 then
			ReplicatedStorage.API["HousingAPI/ActivateFurniture"]:InvokeServer(Players[Player.Name], lur, "UseBlock", {["reward"]=""}, Player.Character)
			wait()
			for i,v in pairs(require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.food) do
				if v.id == lures_bait[2] then
					ReplicatedStorage.API["HousingAPI/ActivateFurniture"]:InvokeServer(Players[Player.Name], lur, "UseBlock", {["bait_unique"]=i, ["bait_kind"]=lures_bait[2]}, Player.Character)
					lur_time = os.time()
					break
				end
			end
		end
		wait(1)
	end) if err then con_log(err, "LURE ERROR:") end
end



---[===[
local _,err=pcall(function()
	local licence
	for i,v in pairs(require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.toys) do
		if v.id == "trade_license" then
			licence = true
		end
	end
	if not licence then
		--set_thread_identity(2)
		ReplicatedStorage.API["TradeAPI/BeginQuiz"]:FireServer()
		con_log("LICENSE:")
		wait(1)
		for i,v in pairs(getgc(true)) do
			local tmp
			pcall(function()
				tmp = v.question_index
				--tmp = rawget(v,"question_index")
			end)
			--if type(v) == "table" and rawget(v,"question_index") then
			if type(v) == "table" and tmp then
				if type(tmp) == "number" then
					for ii,vv in pairs(v.quiz) do
						con_log("",ii)
						ReplicatedStorage.API:FindFirstChild("TradeAPI/AnswerQuizQuestion"):FireServer(vv.answer)
					end
					break
				end
			end
		end
	end
end)if err then
	con_log(err, "error license:")
	--set_thread_identity(7)
end
--]===]

ReplicatedStorage.API:FindFirstChild("HousingAPI/SetDoorLocked"):InvokeServer(true)


local function care(c, baby)
	if Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA("Model") and Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA("Model").Name == Player.Name then
		bed = Furniture(Beds)
		wash = Furniture(Washings)
		toil = Furniture({"Toilet"})
	end
	local fur = { ["sleepy"] = bed, ["dirty"] = wash, ["toilet"] = toil }
	local tmp_use = {["sleepy"] = "UseBlock", ["dirty"] = "UseBlock", ["toilet"] = "Seat1"}
	spawn(function()
		if baby then
			ReplicatedStorage.API["HousingAPI/ActivateFurniture"]:InvokeServer(Player, fur[c], "UseBlock", {["cframe"] = Player.Character.HumanoidRootPart.CFrame}, Player.Character)
		else
			ReplicatedStorage.API["HousingAPI/ActivateFurniture"]:InvokeServer(Player, fur[c], tmp_use[c], {["cframe"] = Player.Character.HumanoidRootPart.CFrame}, GetCurrentPetOut().base.pet_model.Parent)
		end
	end)
	local lim
	if c == "sleepy" then
		lim = 17
	else
		lim = 8
	end
	local t = os.time()
	while os.time()-t < lim do
		wait(.1)
		if baby then
			if not require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.baby_ailments[c] then
				break
			end
		elseif require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID] then
			if not require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID][c] then
				break
			end
		else
			break
		end
	end
	if baby then
		--ReplicatedStorage.API["AdoptAPI/BabyJump"]:FireServer(Player.Character)
		ReplicatedStorage.API["AdoptAPI/ExitSeatStates"]:FireServer()
	end
end


local work
work = {
	["sleepy"] = function(baby)
		if not baby then
			local pref = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID].sleepy.components.preference
			if pref then
				pcall(function()
				--game:HttpGet("https://api.telegram.org/bot".."1064051547:AAFjhTLue8vE4dLEKsdg659QMoY-VQStfAo".."/sendMessage?chat_id=".."543073151".."&text=sleepy: "..tostring(pref.item_kind).." ("..Player.Name..")")
				end)
			end
		end
		care("sleepy", baby)
	end,
	["dirty"] = function(baby)
		if not baby then
			local pref = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID].dirty.components.preference
			if pref then
				pcall(function()
				--game:HttpGet("https://api.telegram.org/bot".."1064051547:AAFjhTLue8vE4dLEKsdg659QMoY-VQStfAo".."/sendMessage?chat_id=".."543073151".."&text=dirty: "..tostring(pref.item_kind).." ("..Player.Name..")")
				end)
			end
		end
		care("dirty", baby)
	end,
	["toilet"] = function(baby)
		local pref = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID].toilet.components.preference
		if pref then
			pcall(function()
			--game:HttpGet("https://api.telegram.org/bot".."1064051547:AAFjhTLue8vE4dLEKsdg659QMoY-VQStfAo".."/sendMessage?chat_id=".."543073151".."&text=toilet: "..tostring(pref.item_kind).." ("..Player.Name..")")
			end)
		end
		care("toilet", false)
	end,
	["hungry"] = function(baby)
		if baby then
			for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.food do
				if table.find(eating, v.id) then
					ReplicatedStorage.API["ToolAPI/ServerUseTool"]:FireServer(i, "END")
					break
				end
			end
		else
			local pref = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID].hungry.components.preference
			if pref then
				if pref.item_kind == "pizza_shop_pizza" then
					pref = nil
				end
			end
			if pref then
				ReplicatedStorage.API["ShopAPI/BuyItem"]:InvokeServer("food", pref.item_kind, {})
				pref = {pref.item_kind}
			else
				pref = food
			end
			while type(pref) ~= "string" and farm do
				wait()
				for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.food do
					if table.find(pref, v.id) then
						pref = i
						break
					end
				end
				if type(pref) ~= "string" then
					if f_bowl then
						pref = "UseBowl"
					elseif pref ~= eating then
						pref = eating
					else
						local res = ReplicatedStorage.API["ShopAPI/BuyItem"]:InvokeServer("food", "pizza", {})
						pref = food
						if res == "too many purchased in server" then
							ReplicatedStorage.API["ShopAPI/BuyItem"]:InvokeServer("food", "apple", {})
						end
					end
				end
			end
			-------
			if pref ~= "UseBowl" and type(pref) == "string" then
				con_log(require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.food[pref].id)
			else
				con_log(pref[1], pref)
			end
			-------
			if pref == "UseBowl" then
				ReplicatedStorage.API["HousingAPI/ActivateFurniture"]:InvokeServer(Player, f_bowl, "UseBlock",
						{["cframe"] = Player.Character.HumanoidRootPart.CFrame}, GetCurrentPetOut().base.pet_model.Parent)
			elseif type(pref) == "string" then
				if table.find({"pet_tread", "camping_2023_smores", "smores"}, require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.food[pref].id) then
					Player.Character:FindFirstChildWhichIsA("Humanoid"):ChangeState("Jumping")
					wait(.05)
				end
				ReplicatedStorage:FindFirstChild("PetObjectAPI/CreatePetObject",true):InvokeServer("__Enum_PetObjectCreatorType_2", {["unique_id"] = pref, ["pet_unique"] = PetID})
				--ReplicatedStorage:FindFirstChild("PetObjectAPI/CreatePetObject",true):InvokeServer("__Enum_PetObjectCreatorType_2",{["unique_id"] = pref,["possible_grabber_pet_unique"] = PetID,["pet_unique"] = PetID,["grab_immediately"] = false})
				--ReplicatedStorage.API["PetAPI/ConsumeFoodItem"]:FireServer(pref, PetID)
			end
			wait(8)
			ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(PetID)
			ReplicatedStorage.API["ToolAPI/Equip"]:InvokeServer(PetID)
		end
	end,
	["thirsty"] = function(baby)
		if baby then
			--too many purchased in server
			--ReplicatedStorage.API["ShopAPI/BuyItem"]:InvokeServer("food", "soda_fountain_water", {})
			for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.food do
				if v.id == "soda_fountain_water" or v.id == "soda_fountain_soda" then
					for _=1,4 do
						ReplicatedStorage.API["ToolAPI/ServerUseTool"]:FireServer(i, "END")
					end
					break
				end
			end
		else
			local pref = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID].thirsty.components.preference
			pcall(function()
			--game:HttpGet("https://api.telegram.org/bot".."1064051547:AAFjhTLue8vE4dLEKsdg659QMoY-VQStfAo".."/sendMessage?chat_id=".."543073151".."&text=thirsty: "..tostring(pref.item_kind).." ("..Player.Name..")")
			end)
			--[==[
			if pref then
				if pref.item_kind == "pizza_shop_pizza" then
					pref = nil
				end
			end
			if pref then
				ReplicatedStorage.API["ShopAPI/BuyItem"]:InvokeServer("food", pref.item_kind, {})
				pref = {pref.item_kind}
			else
				pref = drink
			end
			--]==]
			
			pref = drink
			while type(pref) ~= "string" and farm do
				wait()
				for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.food do
					if table.find(pref, v.id) then
						pref = i
						break
					end
				end
				if type(pref) ~= "string" then
					if w_bowl then
						pref = "UseBowl"
					elseif pref == drink then
						pref = {"soda_fountain_soda", "soda_fountain_water"}
					else
						local res = ReplicatedStorage.API["ShopAPI/BuyItem"]:InvokeServer("food", "water_paper_cup", {})
						pref = {"water_paper_cup"}
						if res == "too many purchased in server" then
							ReplicatedStorage.API["ShopAPI/BuyItem"]:InvokeServer("food", "tea", {})
						end
					end
				end
			end
			-------
			if pref ~= "UseBowl" and type(pref) == "string" then
				con_log(require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.food[pref].id)
			else
				con_log(pref[1], pref)
			end
			-------
			if pref == "UseBowl" then
				ReplicatedStorage.API["HousingAPI/ActivateFurniture"]:InvokeServer(Player, w_bowl, "UseBlock",
						{["cframe"] = Player.Character.HumanoidRootPart.CFrame}, GetCurrentPetOut().base.pet_model.Parent)
			elseif type(pref) == "string" then
				ReplicatedStorage:FindFirstChild("PetObjectAPI/CreatePetObject",true):InvokeServer("__Enum_PetObjectCreatorType_2", {["unique_id"] = pref, ["pet_unique"] = PetID})
			end
			wait(8)
			ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(PetID)
			ReplicatedStorage.API["ToolAPI/Equip"]:InvokeServer(PetID)
		end
	end,
	["play"] = function(baby)
		local pref = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID].play.components.preference
		if pref then
			pcall(function()
			--game:HttpGet("https://api.telegram.org/bot".."1064051547:AAFjhTLue8vE4dLEKsdg659QMoY-VQStfAo".."/sendMessage?chat_id=".."543073151".."&text=play: "..tostring(pref.item_kind).." ("..Player.Name..")")
			end)
		end
		for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.toys do
			if v.id == "squeaky_bone_default" then
				ReplicatedStorage:FindFirstChild("PetObjectAPI/CreatePetObject",true):InvokeServer("__Enum_PetObjectCreatorType_1",{["unique_id"] = i,["possible_grabber_pet_unique"] = PetID,["pet_unique"] = PetID,["grab_immediately"] = false})
				ReplicatedStorage:FindFirstChild("PetObjectAPI/DropPetObject",true):FireServer(i, PetID)
				local t = os.time()
				while not GetCurrentPetOut().move_state.is_moving and farm do
					wait()
					if os.time()-t > 10 then
						return
					end
				end
				while GetCurrentPetOut().move_state.is_moving and farm do
					wait()
				end
				ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(PetID)
				ReplicatedStorage.API["ToolAPI/Equip"]:InvokeServer(PetID)
				break
			end
		end
	end,
	["walk"] = function()
		local pref = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID].walk.components.preference
		if pref then
			pcall(function()
			--game:HttpGet("https://api.telegram.org/bot".."1064051547:AAFjhTLue8vE4dLEKsdg659QMoY-VQStfAo".."/sendMessage?chat_id=".."543073151".."&text=walk: "..tostring(pref.item_kind).." ("..Player.Name..")")
			end)
		end
		local last_pos = Player.Character.HumanoidRootPart.CFrame
		for i=1,40 do
			Player.Character:FindFirstChildWhichIsA("Humanoid"):MoveTo(Player.Character.HumanoidRootPart.Position + Vector3.new(20*(-1)^i, 0, 20*(-1)^i))
			wait(2)
			----con_log(GetCurrentPetOut().move_state.stopped_moving_time - GetCurrentPetOut().move_state.last_move_time)----
			if GetCurrentPetOut().move_state.stopped_moving_time - GetCurrentPetOut().move_state.last_move_time > 7 then
				break
			end
			if not require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID] then
				break
			elseif not require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID].walk then
				break
			end
		end
		Player.Character.HumanoidRootPart.CFrame = last_pos
	end,
	["ride"] = function()
		local pref = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID].ride.components.preference
		if pref then
			pcall(function()
			--game:HttpGet("https://api.telegram.org/bot".."1064051547:AAFjhTLue8vE4dLEKsdg659QMoY-VQStfAo".."/sendMessage?chat_id=".."543073151".."&text=ride: "..tostring(pref.item_kind).." ("..Player.Name..")")
			end)
		end
		local veh
		for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.strollers do
			if v.id == "stroller-default" then
				veh = i
				break
			end
		end
		ReplicatedStorage.API["ToolAPI/Equip"]:InvokeServer(veh)
		ReplicatedStorage.API["AdoptAPI/UseStroller"]:InvokeServer(Player, GetCurrentPetOut().base.pet_model.Parent, Player.Character.StrollerTool.ModelHandle.TouchToSits.TouchToSit)
		local last_pos = Player.Character.HumanoidRootPart.CFrame
		for i=1,16 do
			Player.Character:FindFirstChildWhichIsA("Humanoid"):MoveTo(Player.Character.HumanoidRootPart.Position + Vector3.new(20*(-1)^i, 0, 20*(-1)^i))
			wait(2)
			if not require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID] then
				break
			elseif not require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID].ride then
				break
			end
		end
		Player.Character.HumanoidRootPart.CFrame = last_pos
		ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(PetID)
		wait()
		ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(veh)
		wait()
		ReplicatedStorage.API["ToolAPI/Equip"]:InvokeServer(PetID)
	end,
	["pet_me"] = function()
		set_thread_identity(2)
		for i,v1 in Player.PlayerGui:GetChildren() do
			if v1.Name == "ailments_list" then
				local _,err=pcall(function()
					--getconnections(v1.SurfaceGui.Container.Ailment.MouseButton1Click)[1]:Fire()
					getconnections(v1.SurfaceGui.Container.Ailment.MouseButton1Click)[1].Function()
				end)
				if err then
					set_thread_identity(7)
					keypress(0x45)
					wait(.1)
					keyrelease(0x45)
					set_thread_identity(2)
				end
			end
		end
		wait(1)
		ReplicatedStorage.API["AilmentsAPI/ProgressPetMeAilment"]:FireServer(PetID)
		wait(1)
		local _,err=pcall(function()
			--getconnections(Player.PlayerGui.FocusPetApp:FindFirstChild("BackButton",true).MouseButton1Down)[1]:Fire()
			--getconnections(Player.PlayerGui.FocusPetApp:FindFirstChild("BackButton",true).MouseButton1Up)[1]:Fire()
			--getconnections(Player.PlayerGui.FocusPetApp:FindFirstChild("BackButton",true).MouseButton1Click)[1]:Fire()
			getconnections(Player.PlayerGui.FocusPetApp:FindFirstChild("BackButton",true).MouseButton1Down)[1].Function()
			getconnections(Player.PlayerGui.FocusPetApp:FindFirstChild("BackButton",true).MouseButton1Up)[1].Function()
			getconnections(Player.PlayerGui.FocusPetApp:FindFirstChild("BackButton",true).MouseButton1Click)[1].Function()
		end)
		set_thread_identity(7)
		if err then
			keypress(0x45)
			wait(.1)
			keyrelease(0x45)
		end
	end,
}

local function red_work(c)
	if Workspace.Interiors:FindFirstChildWhichIsA("Model") then
		--if Workspace.Interiors:FindFirstChildWhichIsA("Model").Name ~= red_name[c] then
		if not Workspace.Interiors:FindFirstChildWhichIsA("Model").Name:find(red_name[c]) then
			con_log("tp1")----
			GoToStore(red_name[c])
		end
	else
		con_log("tp2")----
		GoToStore(red_name[c])
	end
	if c == "sick" then
		ReplicatedStorage.API["HousingAPI/ActivateInteriorFurniture"]:InvokeServer(Furniture({"Doctor"}), "UseBlock", "Yes", Player.Character)  --"f-80"
	end
	if c == "summerfest_bonfire" then
		Player.Character.HumanoidRootPart.CFrame = CFrame.new(Vector3.new(-570,50,-1540))
		Workspace.Pstone.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,-3,0))
	end
	if c == "buccaneer_band" then
		Player.Character.HumanoidRootPart.CFrame = CFrame.new(Vector3.new(-600,50,-1635))
		Workspace.Pstone.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,-3,0))
		local n = 0
		local inst = {"Marimba", "Accordion", "Guitar", "Drums"}
		local res
		while true do
			n = n + 1
			if n > 4 then
				n = 1
			end
			con_log(n)-----
			res = true
			spawn(function()
				local _,err=pcall(function()
				res=ReplicatedStorage.API["HousingAPI/ActivateInteriorFurniture"]:InvokeServer(Furniture({"summerfest_2025_buccaneer_band"}), inst[n], {["cframe"] = Player.Character.HumanoidRootPart.CFrame * CFrame.Angles(3, -1, 3)}, Player.Character)  --"f-36"
				end)if err then con_log(err)end
			end)
			wait(2)
			if res then
				break
			end
		end
		if res then
			local chk
			local t = os.time()
			while os.time()-t < 50 and (require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.baby_ailments[c] or not chk) do
				wait(.1)
				if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID] and not chk then
					if not require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID][c] then
						chk = true
					end
				end
			end
			Player.Character:FindFirstChildWhichIsA("Humanoid"):ChangeState("Jumping")
			ReplicatedStorage.API["AdoptAPI/ExitSeatStates"]:FireServer()
		end
	end
	wait(2)
end


local function mystery(n)
	set_thread_identity(2)
	local red, street
	for i,v1 in Player.PlayerGui:GetChildren() do
		if v1.Name == "ailments_list" then
			pcall(function()
				--getconnections(v1.SurfaceGui.Container.Ailment.MouseButton1Click)[1]:Fire()
				getconnections(v1.SurfaceGui.Container.Ailment.MouseButton1Click)[1].Function()
			end)
		end
	end
	wait(1)
	local elems = {}
	for i,v2 in Player.PlayerGui.FocusPetApp.Frame.Ailments:GetChildren() do
		if v2.Name == "AilmentContainer" then
			table.insert(elems, v2)
		end
	end
	for i,v3 in n do
		if i == 1 then
			--getconnections(elems[1].End.MouseButton1Click)[1]:Fire()
			getconnections(elems[1].End.MouseButton1Click)[1].Function()
			wait(.3)
			local _,err=pcall(function()
				getconnections(Player.PlayerGui.FocusPetApp.Frame.Tooltip.AilmentCard.MouseButton1Click)[1].Function()
				--getconnections(Player.PlayerGui.FocusPetApp.Frame.Tooltip.AilmentCard.MouseButton1Click)[1]:Fire()
				wait(.1)
			end)--if err then con_log(err, "ERROR open")end
			--wait(1)
			--getconnections(Player.PlayerGui.FocusPetApp.Frame.Backpack.OpenBackpackContainer.OpenBackpack.MouseButton1Click)[1]:Fire()
			--getconnections(Player.PlayerGui.FocusPetApp.Frame.Backpack.OpenBackpackContainer.OpenBackpack.MouseButton1Click)[1]:Fire()
			getconnections(Player.PlayerGui.FocusPetApp.Frame.Backpack.OpenBackpackContainer.OpenBackpack.MouseButton1Click)[1].Function()
			getconnections(Player.PlayerGui.FocusPetApp.Frame.Backpack.OpenBackpackContainer.OpenBackpack.MouseButton1Click)[1].Function()
			wait()
		end
		con_log(v3[1])
		--getconnections(elems[v3[1]].End.MouseButton1Click)[1]:Fire()
		getconnections(elems[v3[1]].End.MouseButton1Click)[1].Function()
		wait(.5)
		local _,err=pcall(function()
			getconnections(Player.PlayerGui.FocusPetApp.Frame.Tooltip.AilmentCard.MouseButton1Click)[1].Function()
			--getconnections(Player.PlayerGui.FocusPetApp.Frame.Tooltip.AilmentCard.MouseButton1Click)[1]:Fire()
			wait(1)
		end)--if err then con_log(err, "ERROR open")end
		--if true then return end-----
		local chk
		for n2=2,4 do
			if table.find(event_task, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[n2].Container.Content.Text.AilmentName.Text]) and event_on then
				break
			end
			if Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[n2].Container.Content.Text.Frame.Bonus.Visible then
				con_log(cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[n2].Container.Content.Text.AilmentName.Text],"BONUS:")
				--ReplicatedStorage.API["AilmentsAPI/ChooseMysteryAilment"]:FireServer(v3[3], n2-1, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[n2].Container.Content.Text.AilmentName.Text])
				ReplicatedStorage.API["AilmentsAPI/ChooseMysteryAilment"]:FireServer(PetID, 'mystery', n2-1, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[n2].Container.Content.Text.AilmentName.Text])
				wait(1)
				chk = true
				break
			end
		end
		if not chk then
			if table.find(street_task, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[2].Container.Content.Text.AilmentName.Text]) and
			table.find(street_task, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[3].Container.Content.Text.AilmentName.Text]) and
			table.find(street_task, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[4].Container.Content.Text.AilmentName.Text]) then
				street = v3[3]
			end
			--if table.find(red_task, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[2].Container.Content.Text.AilmentName.Text]) or
			--table.find(red_task, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[3].Container.Content.Text.AilmentName.Text]) or
			--table.find(red_task, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[4].Container.Content.Text.AilmentName.Text]) then
				--con_log(i, "passed:")
				--wait(.5)
			--else
				for n2=2,4 do
					if not table.find(street_task, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[n2].Container.Content.Text.AilmentName.Text]) then
						if table.find(red_task, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[2].Container.Content.Text.AilmentName.Text]) or
						table.find(red_task, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[3].Container.Content.Text.AilmentName.Text]) or
						table.find(red_task, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[4].Container.Content.Text.AilmentName.Text]) then
							red = true
						end
						if not red or (red and table.find(red_task, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[n2].Container.Content.Text.AilmentName.Text])) then
							con_log(cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[n2].Container.Content.Text.AilmentName.Text].." = "..tostring(n2-1),"OPEN: "..v3[3])
							--ReplicatedStorage.API["AilmentsAPI/ChooseMysteryAilment"]:FireServer(v3[3], n2-1, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[n2].Container.Content.Text.AilmentName.Text])
							ReplicatedStorage.API["AilmentsAPI/ChooseMysteryAilment"]:FireServer(PetID, 'mystery', n2-1, cards[Player.PlayerGui.FocusPetApp.ChooseAilment.Choices:GetChildren()[n2].Container.Content.Text.AilmentName.Text])
							wait(1)
							break
						end
					end
				end
			--end
		end
		--getconnections(Player.PlayerGui.FocusPetApp.Frame.Backpack.OpenBackpackContainer.OpenBackpack.MouseButton1Click)[1]:Fire()
		--getconnections(Player.PlayerGui.FocusPetApp.Frame.Backpack.OpenBackpackContainer.OpenBackpack.MouseButton1Click)[1]:Fire()
		getconnections(Player.PlayerGui.FocusPetApp.Frame.Backpack.OpenBackpackContainer.OpenBackpack.MouseButton1Click)[1].Function()
		getconnections(Player.PlayerGui.FocusPetApp.Frame.Backpack.OpenBackpackContainer.OpenBackpack.MouseButton1Click)[1].Function()
		wait()
		if red then
			break
		end
	end
	pcall(function()
		--getconnections(Player.PlayerGui.FocusPetApp:FindFirstChild("BackButton",true).MouseButton1Down)[1]:Fire()
		--getconnections(Player.PlayerGui.FocusPetApp:FindFirstChild("BackButton",true).MouseButton1Up)[1]:Fire()
		--getconnections(Player.PlayerGui.FocusPetApp:FindFirstChild("BackButton",true).MouseButton1Click)[1]:Fire()
		getconnections(Player.PlayerGui.FocusPetApp:FindFirstChild("BackButton",true).MouseButton1Down)[1].Function()
		getconnections(Player.PlayerGui.FocusPetApp:FindFirstChild("BackButton",true).MouseButton1Up)[1].Function()
		getconnections(Player.PlayerGui.FocusPetApp:FindFirstChild("BackButton",true).MouseButton1Click)[1].Function()
	end)
	set_thread_identity(7)
	return street
end




local function endswith(str, ending)
   return ending == "" or str:sub(-#ending) == ending
end

local neon_enabled

local function Pets()
	if not PetID and GetCurrentPetOut() then
		PetID = string.split(GetCurrentPetOut().unique_id, "-")
		PetID = table.concat(PetID, "-", 1, #PetID-1)
	else
		local items = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets
		local ierar = {}
		local tmp
		for i,n in pairs(items) do
			if n.id == "practice_dog" then
				if PetID ~= n.unique then
					tmp = n.unique
				end
			elseif n.id:find("minigame") then
			else
				local t1 = n.properties.xp
				if t1 < 1000 then
					t1 = "0"..tostring(t1)
				else
					t1 = tostring(t1)
				end
				t1 = tostring(n.properties.age)..t1
				if n.properties.neon and neon_enabled then
					t1 = "1"..t1
				else
					t1 = "0"..t1
				end
				
				t1 = "0"..t1
				table.insert(ierar, {tonumber(t1), n.unique, n.id})
			end
		end
		table.sort(ierar, function (a, b) return a[1] > b[1] end)

		local tmp_flag = true
		for i,v in pairs(ierar) do
			if tmp_flag then
				tmp = items[ v[2] ]["unique"]
				tmp_flag = false
			end
			--con_log(items[ v[2] ]["properties"]["ailments_completed"], items[ v[2] ]["id"])--
			--wait(2)--
			--if items[ v[2] ]["properties"]["age"] ~= 6 then
			PetID = tmp
			if items[ v[2] ]["properties"]["age"] ~= 6 and not endswith(items[ v[2] ]["id"], "_egg") and table.find(worked_pets, items[ v[2] ]["id"]) then
				PetID = items[ v[2] ]["unique"]
				break
			end
		end
		if not PetID then
			PetID = tmp
		end
		if not items[PetID] then	--for particle dog, if no one pets
			PetID = tmp
		end
	end
	
	ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(PetID)
	ReplicatedStorage.API["ToolAPI/Equip"]:InvokeServer(PetID)
	wait(1)
	Pet = GetCurrentPetOut().base.pet_model.Parent
	--con_log(PetID, "")
end





--local trans_cash = false
local Bankers = {}
local tmp_Bankers = {}
local home_after_cash = false
local info_bank = {}
local kassa = {}

local Ps = {}
local PetsList = {}
local make_neon = false

local visor


local Fluent
local _,err=pcall(function()
	Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
end)
if err then
	con_log(err)
	wait(2)
	Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
end

local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()


local Window = Fluent:CreateWindow({
    Title = "AdoptMe: Game ",
    SubTitle = "ver. 2.8.13",
    TabWidth = 160,
    Size = UDim2.fromOffset(500, 450),
    Acrylic = false, -- The blur may be detectable, setting this to false disables blur entirely
    Theme = "Darker",
    MinimizeKey = Enum.KeyCode.LeftControl -- Used when theres no MinimizeKeybind
})



local Tabs = {
    Main = Window:AddTab({ Title = "Main", Icon = "" }),
    Manag = Window:AddTab({ Title = "Manager", Icon = "" }),
    AgeUp = Window:AddTab({ Title = "Age-Up", Icon = "" }),
	--Events = Window:AddTab({ Title = "Events", Icon = "" }),
	Cash = Window:AddTab({ Title = "Cashregister", Icon = "" }),
	Stat = Window:AddTab({ Title = "Statistics", Icon = "" }),
    --Settings = Window:AddTab({ Title = "Settings", Icon = "settings" })
}

local Options = Fluent.Options

do
	--Fluent:Notify({
    --    Title = "V.G Hub Loaded",
    --    Content = "Congrats your using V.G Hub ",
    --    SubContent = "", -- Optional
    --    Duration = 10 -- Set to nil to make the notification not disappear
    --})
        
    local Toggle = Tabs.Main:AddToggle("Farm", {Title = "Farm", Default = false})
    Toggle:OnChanged(function()
		if Options.Farm.Value and exper then
			con_log("", "Farm started")
			--spawn(function()
			ReplicatedStorage.API["TeamAPI/ChooseTeam"]:InvokeServer("Babies", {},true)
			--ReplicatedStorage.API["FamilyAPI/CreateFamily"]:InvokeServer()
			local n = 0
			while wait(1) do
				n = n + 1
				if Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA("Model") and Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA("Model").Name == Player.Name then
					break
				end
				if n > 20 then
					con_log("maybe something is broken", "ERROR!")
					break
				end
			end
			
			local _,err=pcall(function()
			Pets()
			end)
			if err then
			con_log(err)
			end
			
			bed = Furniture(Beds)
			wash = Furniture(Washings)
			toil = Furniture({"Toilet"})
			f_bowl = Furniture(bowls_f)
			w_bowl = Furniture(bowls_w)
			wait(2)
			farm = true
			Player.Character.HumanoidRootPart.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,100,0))
			Workspace.Pstone.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,-3,0))
			--end)
		elseif farm then
			con_log("", "Farm stopped")
			farm = false
			busy = false
			if not Options.AU.Value then
				--ReplicatedStorage.API["TeamAPI/ChooseTeam"]:InvokeServer("Babies", {},true)
			end
		end
    end)

	

    
	local def_vol
	pcall(function()
		def_vol = readfile("egg.txt")
	end)
	if not def_vol then
		def_vol = 1
	end
	local Toggle = Tabs.Main:AddToggle("hatch_egg", {Title = "Hatch Eggs", Default = true})
    Toggle:OnChanged(function()
	end)
	local Dropdown = Tabs.Main:AddDropdown("eggs", {
		Title = "egg to hatch:",
		Values = eggs,
		Multi = false,
		Default = def_vol,
	})
	Dropdown:OnChanged(function(egg_index)
		Options.eggs.Close()
		pcall(function()
			writefile("egg.txt", tostring(egg_index))
		end)
	end)
	
	local def_vol
	pcall(function()
		def_vol = readfile("give_item.txt")
	end)
	if def_vol == "false" then
		def_vol = false
	else
		def_vol = true
	end
	local Toggle = Tabs.Cash:AddToggle("give_items", {Title = "Give Items", Default = def_vol})
    Toggle:OnChanged(function()
		pcall(function()
			writefile("give_item.txt", tostring(Options.give_items.Value))
		end)
	end)
	
	pcall(function()
		visor = readfile("host_visor.txt")
	end)
	local Input = Tabs.Main:AddInput("v_serv", {
		Title = "ip address : port",
		Default = visor,
		--Placeholder = "Placeholder",
		--Numeric = false,
		--Finished = false,
	})
    Input:OnChanged(function()
        visor = Input.Value
		pcall(function()
			writefile("host_visor.txt", visor)
		end)
    end)	
	
	pcall(function()
		local tmp = readfile("trade.txt")
		if tmp ~= "" then
			t_user = tmp
		end
	end)
	local Input = Tabs.Main:AddInput("trade_usr", {
		Title = "Trade with:",
		Default = t_user,
		--Placeholder = "Placeholder",
		--Numeric = false,
		--Finished = false,
	})
    Input:OnChanged(function()
        t_user = Input.Value
		writefile("trade.txt", t_user)
    end)
	Tabs.Main:AddButton({Title = "Send trade", --Description = "",
        Callback = function()
			spawn(function()
				local _,err=pcall(function()
					if t_user then
						ReplicatedStorage.API["TradeAPI/SendTradeRequest"]:FireServer(Players[t_user])
					end
				end)
				if err then
					con_log(err, "Trade error:")
				end
			end)
		end
    })		
	
	local Toggle = Tabs.Main:AddToggle("with_bottle", {Title = "Use Age-Up Potions", Default = false})
    Toggle:OnChanged(function()
	end)
	
	
	--[[
	local Toggle = Tabs.Main:AddToggle("Event1", {Title = "Minigame 1", Default = false})
    Toggle:OnChanged(function()
		if Options.Event1.Value then
			--Options.Event2:SetValue(true)
		else
			--Options.Event2:SetValue(false)
		end
	end)
	--]]
	
	
	-------------- EVENTS ------------------
	--[[
	local Toggle = Tabs.Events:AddToggle("Event3", {Title = "BUY Shark", Default = false})
    Toggle:OnChanged(function()
		if Options.Event3.Value then
			--local tmp = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].social_stones_2025
			local tmp = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].cranky_coins_2025
			if tmp then
				if tmp >= 11000 then
					ReplicatedStorage.API["ShopAPI/BuyItem"]:InvokeServer('pets', "summerfest_2025_hammerhead_shark", {["buy_count"]=tmp//11000})
				end
			end
			Options.Event3:SetValue(false)
		end
	end)
	--]]
    
		
		
		
		
		--------------Manager-----------------
	
	local manager
	pcall(function()
		manager = readfile("host.txt")
	end)
	local Input = Tabs.Manag:AddInput("m_serv", {
		Title = "ip address and port",
		Default = manager,
		--Placeholder = "Placeholder",
		--Numeric = false,
		--Finished = false,
	})
    Input:OnChanged(function()
        manager = Input.Value
    end)
	
	local started
	local m_bankers = {}
	local kassa = {}
	local maniq = {}
	local bank_mode
	local m_timer = 0
	local users = {}
    local Toggle = Tabs.Manag:AddToggle("Trans", {Title = "Transfer bucks", Default = false})--manager~=""})
	Toggle:OnChanged(function()
		--spawn(function()
			started = Options.Trans.Value
			con_log(started, "transfer:")----
			Tabs.Manag.Container:GetDescendants()[23].TextSize = 34
		--end)
		if started and manager and exper then
			local _,err=pcall(function()
				writefile("host.txt", manager)
				while started and wait(1) and not Fluent.Unloaded do
					local _,err=pcall(function()
						----
						if #m_bankers > 0 then
							local tmp_tmr = {}
							for _,usr in m_bankers do
								table.insert(tmp_tmr, usr[1])
							end
							--con_log(os.time() - m_timer, #m_bankers)
							--Options.Trans:SetTitle("Transfer bucks "..tostring(os.time() - m_timer).." sec.")
							--Options.Trans:SetTitle("after "..tostring(math.min(unpack(tmp_tmr)) - os.time()).." sec.")
							Options.Trans:SetTitle("after "..tostring(math.max(unpack(tmp_tmr)) - os.time()).." sec.")
						end
						----
						local _,err=pcall(function()
							local bal
							local n = 0
							bal = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].money
							for i, Potion in next,require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.food do
								if Potion.id == "pet_age_potion" then
									n = n + 1
								end
							end
							local res = game:HttpGet("http://"..manager.."/ready&"..Player.Name.."="..tostring(bal).."&Potion="..tostring(n))
							local tmp = string.split(res, ":")
							users = {}
							if res ~= "" then
								for _,i in tmp do
									users[i:split("=")[1]] = i:split("=")[2]
									local chk = true
									for _,usr in m_bankers do
										if usr[2] == i:split("=")[1] then
											chk = false
										end
									end
									if chk then
										table.insert(m_bankers, {os.time(), i:split("=")[1]})
										table.insert(m_bankers, {os.time(), i:split("=")[1]})
										table.insert(m_bankers, {os.time(), i:split("=")[1]})
									end
								end
								for i,usr in m_bankers do
									if not users[usr[2]] then
										m_bankers[i] = nil
									elseif users[usr[2]] == "3" and os.time() - i > 2 then
										m_bankers[i] = nil
										table.insert(m_bankers, {os.time(), usr[2]})
									end
								end
							else
								for i,v in m_bankers do
									m_bankers[i] = nil
									maniq[v[2]] = nil
									kassa[v[2]] = nil
								end
							end
						end)if err then con_log(err) end
						
						for _,usr in m_bankers do
							local banker = usr[2]
							local chk_respons
							local _,err=pcall(function()
								if usr[1] - os.time() <= 0 then
									local ready,ans=pcall(function()
										local tmp = Players[banker]
										local tmp = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[banker].char_wrapper_raw.location
									end)if ans then con_log(ans, banker) end
									if ready then
										local ready = true
										if users[banker] ~= "0" then
											--con_log(kassa[banker])-------
											if users[banker] == "3" or (users[banker] == "1" and not kassa[banker]) or (users[banker] == "2" and not maniq[banker]) then
												local scan_timeout = os.time()
												while true do
													ready = true
													local home = Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA("Model")
													if not home or home.Name ~= banker then
														if busy then
															ready = false
														else
															--busy = true
															local _,err=pcall(function()
																--wait(2)
																con_log(banker.." ("..tostring(require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].char_wrapper_raw.location.destination_id)..")", "Teleport to:")
																local tmp = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[banker].char_wrapper_raw.location
																busy = true
																if not require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].house_interior.furniture or
																require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].char_wrapper_raw.location.destination_id == "Neighborhood" then
																	ready = false
																else
																	GoToHome(banker)
																end
															end)
															if err then
																ready = false
																con_log(err)
															end
															busy = false
														end
													end
													if ready then
														local prs
														local tr_mode
														for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].house_interior.furniture do
															if v.id == "mannequins_2024_mannequin" then
																if v.price == 100 then
																	maniq[banker] = {i, v.outfit_version, v.outfit_name}
																	tr_mode = "2"
																else
																	prs = v.price
																end
															end
														end
														if not maniq[banker] then
															if prs then
																con_log(prs, "price for mannequin not 100:")
															else
																if users[banker] ~= "2" then
																	con_log("search cashbox...", "mannequins not found")
																	kassa[banker] = Furniture(cashbox)
																	if not kassa[banker] then
																		con_log("cashregister not found!")
																		ready = false
																	else
																		tr_mode = "1"
																	end
																else
																	con_log("mannequins not found!")
																	wait(1)
																	ready = false
																end
															end
														end
														if (tr_mode and users[banker] == "3") or (tr_mode == "2" and tr_mode ~= users[banker]) then
															con_log(tr_mode, users[banker])------
															local _,err=pcall(function()
																game:HttpGet("http://"..manager.."/mode="..banker.."="..tr_mode)
																users[banker] = tr_mode
															end)if err then con_log(err) end
														end
													end
													if users[banker] == "3" and not maniq[banker] and not kassa[banker] and os.time()-scan_timeout < 40 then
														con_log(os.time()-scan_timeout)
														wait(1)
													else
														break
													end
												end
											end
											if ready then
												local res
												local _,err=pcall(function()
													res = game:HttpGet("http://"..manager.."/user="..banker)
													chk_respons = true
												end)if err then con_log(err) end
												if res == "busy" then
													wait(1)
												elseif res == "" or res == "error" then
													for i,v in m_bankers do
														if v[2] == banker then
															m_bankers[i] = nil
															maniq[banker] = nil
															kassa[banker] = nil
														end
													end
												elseif string.find(res, banker) then
													local amount = string.split(res, ":")[2]
													if amount == "done" then
														maniq[banker] = nil
														kassa[banker] = nil
														for i,v in m_bankers do
															if v[2] == banker then
																m_bankers[i] = nil
															end
														end
													else
														local tr_mode = string.split(res, ":")[3]
														users[banker] = tr_mode
														local buy
														local bal = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].money
														local tmp_time = os.time()
														if tr_mode == "1" and kassa[banker] then
															local k_chk = string.split(res, ":")[4]
															if not k_chk then
																k_chk = 0
															end
															if tonumber(k_chk) > 0 then
																pcall(function()
																	ReplicatedStorage.API["HousingAPI/ActivateFurniture"]:InvokeServer(Players[banker], kassa[banker], "UseBlock", tonumber(amount), Player.Character)
																	buy = true
																end)
															end
														elseif tr_mode == "2" and maniq[banker] then
															amount = 100
															pcall(function()
																ReplicatedStorage.API["AvatarAPI/DeleteOutfit"]:FireServer("player", maniq[banker][3])
															end)
															local _,err_ch=pcall(function()
																buy = ReplicatedStorage.API["AvatarAPI/BuyMannequinOutfit"]:InvokeServer(Players[banker], maniq[banker][1], maniq[banker][2], tonumber(amount))
																wait()
																ReplicatedStorage.API["AvatarAPI/DeleteOutfit"]:FireServer("player", maniq[banker][3])
															end)if err_ch then con_log(err_ch, 1495)end
														end
														if tr_mode ~= "0" and tr_mode ~= "3" then
															usr[1] = os.time() + 120
															bal = bal - require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].money
															con_log(tostring(bal).." ("..tostring(os.time()-tmp_time).."sec.)", tostring(banker).." send:")
															local check = true
															if not buy then
																check = false
																maniq[banker] = nil
																kassa[banker] = nil
															end
															if buy and bal < 7 and tr_mode == "1" then
																kassa[banker] = nil
															end
															local _,err=pcall(function()
																res = game:HttpGet("http://"..manager.."/done="..banker.."&"..amount.."&"..Player.Name.."&"..tostring(check).."&"..tostring(os.time()-tmp_time))
															end)if err then con_log(err) end
														end
													end
												else
													con_log(res)
												end
												local home = Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA("Model")
												if home and home.Name == banker then
													if not busy then
														busy = true
														local _,err=pcall(function()
															if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].house_interior.furniture then
																GoToHome()
															end
														end)
														busy = false
													end
												end
											end
										end
									else
										for i,v in m_bankers do
											if v[2] == banker then
												m_bankers[i] = nil
												maniq[banker] = nil
												kassa[banker] = nil
											end
										end
										wait(1)
									end
									--[[
									if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].char_wrapper_raw.location.destination_id == "Neighborhood" and
									require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].house_interior.furniture then
										--if not busy then
											busy = true
											wait(2)
											local _,err=pcall(function()
												con_log("babyfarm")----
												ReplicatedStorage.API["TeamAPI/ChooseTeam"]:InvokeServer("Babies", {},true)
												local n = 0
												while n < 20 do
													wait(1)
													n = n + 1
													if Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA("Model") and Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA("Model").Name == Player.Name then
														break
													end
												end
												wait(2)
												Player.Character.HumanoidRootPart.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,100,0))
												Workspace.Pstone.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,-3,0))
											end)
											busy = false
										--end
									end
									--]]
								end
							end)
							if err then
								con_log(err)
								busy = false
								if chk_respons then
									local _,err=pcall(function()
										res = game:HttpGet("http://"..manager.."/done="..banker.."&".."0".."&"..Player.Name.."&"..tostring(false).."&"..tostring(os.time()-os.time()))
									end)if err then con_log(err) end
								end
							end
						end
					
					end)
					----
					if err then
						con_log(err)
						busy = false
					end
					----
				end
			end)
			if err then
				con_log(err)
				busy = false
			end
		end
    end)
	


		--------------AgeUp-------------------
	
	Tabs.AgeUp:AddButton({
        Title = "Reset",
        Description = "Refresh pets list",
        Callback = function()
			spawn(function()
				PetsList = {}
				for _, v in pairs(require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets) do
					if v.properties.age ~= 6 and v.id ~= "practice_dog" and not endswith(v.id, "_egg") then
						if not table.find(PetsList, v.id) then
							table.insert(PetsList, v.id)
						end
					end
				end
				Options.PetsL:SetValues(PetsList)
				Options.PetsL:SetValue(nil)
			end)
		end
    })

	local Dropdown = Tabs.AgeUp:AddDropdown("PetsL", {
		Title = "Pet for Age up",
		Values = PetsList,
		Multi = false,
		--Default = 1,
	})
	--Dropdown:SetValue("feed all")
	
	Dropdown:OnChanged(function(Value)
		Options.PetsL.Close()
	end)

    local Toggle = Tabs.AgeUp:AddToggle("MN", {Title = "MAKE MEGA-NEON", Default = false})
    Toggle:OnChanged(function()
		if Options.MN.Value then
			spawn(function()
				local _,err=pcall(function()
					local Pets = {}
					for uniq, v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets do
						if v.properties.age == 6 and v.properties.neon and not v.properties.mega_neon then
							if not Pets[v.id] then
								Pets[v.id] = {}
							end
							table.insert(Pets[v.id], v.unique)
						end
					end
					for i, v in Pets do
						if #v < 4 then
						else
							local Spec={}
							for n=1, #v//4 do
								local tmp = (n-1)*4
								local res = ReplicatedStorage.API["PetAPI/DoNeonFusion"]:InvokeServer({v[tmp+1], v[tmp+2], v[tmp+3], v[tmp+4]})
								if res then
									con_log(i, "Create mega-neon:")
								end
							end
						end
					end
					--if created then
						Options.MN:SetValue(false)
					--end
				
				end)if err then con_log(err, "Error make Neon:")end
			end)
		end
	end)
					
    local Toggle = Tabs.AgeUp:AddToggle("uf", {Title = "ON/OFF make auto NEON", Default = true})
    Toggle:OnChanged(function()
		make_neon = Options.uf.Value
		--if Options.AU.Value then
			--spawn(function()
			--end)
		--end
	end)
	
    local Toggle = Tabs.AgeUp:AddToggle("AU", {Title = "START procreation", Default = false})
    Toggle:OnChanged(function()
		if Options.AU.Value then--and Options.PetsL.Value then
			spawn(function()

			
				--local pet_id
				--local pet_age
				--local uf_pet_age
				_,err=pcall(function()
					
					
					local first_start = true
					local Pets = {}
					local n=1
					for uniq, v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets do
						if v.properties.age ~= 6 and v.id ~= "practice_dog" and not endswith(v.id, "_egg") then
							Pets[uniq] = v.id
							n=n+1
						end
					end
					con_log(n, "Pets:")----
					for uniq, v in Pets do
						if Options.PetsL.Value and Options.PetsL.Value ~= v then
						else
							local n = 0
							local potns = {}
							local p1 = ""
							--con_log(v.properties.age, v.id)
							--pet_id = v.unique
							--uf_pet_age = v.properties.age
							
							--ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(uniq)
							ReplicatedStorage.API["ToolAPI/Equip"]:InvokeServer(uniq)
							--con_log("scanning potion...")
							local check_pition = false
							for i, Potion in next,require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.food do
								if not Options.AU.Value then
									return
								end
								if Potion.id == "pet_age_potion" then
									n = n + 1
									check_pition = true
									if first_start then
										--con_log("", "boost started")
										first_start = false
										Options.Farm:SetValue(false)
									end
									if n == 11 then
										break
									elseif n == 1 then
										p1 = i
									else
										table.insert(potns, i)
									end
								end
							end
							
							if check_pition then								
								local t=os.time()
								local before = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets[uniq].properties.xp
								--ReplicatedStorage:FindFirstChild("PetObjectAPI/CreatePetObject",true):InvokeServer("__Enum_PetObjectCreatorType_2",{["unique_id"] = Potion.unique,["possible_grabber_pet_unique"] = uniq,["pet_unique"] = uniq,["grab_immediately"] = false})
								ReplicatedStorage:FindFirstChild("PetObjectAPI/CreatePetObject",true):InvokeServer("__Enum_PetObjectCreatorType_2", {
								["additional_consume_uniques"] = {unpack(potns)},
								["pet_unique"] = uniq,
								["unique_id"] = p1
								})										
								--ReplicatedStorage:FindFirstChild("PetAPI/ConsumeFoodItem",true):FireServer(Potion.unique, uniq)
								--if not ultra_fast_potion then
									--ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(uniq)
								--end
								
								local Pets_chk = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets
								con_log("age: "..tostring(Pets_chk[uniq].properties.age), "Pet: "..tostring(Pets_chk[uniq].id))----
								--con_log("bottles: "..tostring(n).."|age: "..tostring(Pets_chk[uniq].properties.age), "Pet: "..tostring(Pets_chk[uniq].id))----
								
								--for _, vvv in pairs(require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets) do
									--if vvv.unique == uniq then
										--con_log("bottles: "..tostring(n).."|age: "..tostring(vvv.properties.age), "Pet: "..tostring(vvv.id))----
										--pet_age = vvv.properties.age
									--end
								--end
								
								while require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets[uniq].properties.xp == before and os.time()-t < 15 and
								require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets[uniq].properties.age ~= 6 do
									wait()
								end
								con_log(os.time()-t, "sec:")
								--if not ultra_fast_potion then
								ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(uniq)
								ReplicatedStorage.API["ToolAPI/Equip"]:InvokeServer(uniq)
								--end
								--if pet_age == 6 then
									--break
								--end
								--wait(1)
								--if n >= 5 then
									--break
								--end
								
							else
								con_log("Potion Age-Up not found!")
								break
							end
							--break
						end
					end
					Options.AU:SetValue(false)
					
				end)if err then con_log(err, "Error Age Up:")end
				
				if make_neon then
					local _,err=pcall(function()
						local Pets = {}
						local created
						for uniq, v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets do
							--if v.properties.age == 6 and v.properties.neon and not v.properties.mega_neon then
							if v.properties.age == 6 and not v.properties.neon and not v.properties.mega_neon then
								if not Pets[v.id] then
									Pets[v.id] = {}
								end
								table.insert(Pets[v.id], v.unique)
							end
						end
						for i, v in Pets do
							if #v < 4 then
							else
								local Spec={}
								for n=1, #v//4 do
									local tmp = (n-1)*4
									local res = ReplicatedStorage.API["PetAPI/DoNeonFusion"]:InvokeServer({v[tmp+1], v[tmp+2], v[tmp+3], v[tmp+4]})
									if res then
										created = true
										con_log(i, "Create neon:")
									end
								end
							end
						end
						if created then
							Options.AU:SetValue(true)
						end
					
					end)if err then con_log(err, "Error make Neon:")end
				end
			
			end)
		end
    end)
	
	
	
	
	local Toggle = Tabs.Cash:AddToggle("buy_eggs", {Title = "Buy seasonal Eggs", Default = false})
    Toggle:OnChanged(function()
		spawn(function()
			while Options.buy_eggs.Value do
				result = ReplicatedStorage.API["ShopAPI/BuyItem"]:InvokeServer("pets", "aztec_egg_2025_aztec_egg", {})
				if result ~= "success" or require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].money < 1500 then --too little money
					Options.buy_eggs:SetValue(false)
					break
				end
			wait()
			end				
		end)
	end)
	
	
	Tabs.Cash:AddButton({
        Title = "Reset",
        Description = "Refresh players list",
        Callback = function()
			--if not Options.Pls.Opened then
			--end
			spawn(function()
				Ps = {}
				for i,v in next, Players:GetPlayers() do
					table.insert(Ps, v.Name)
				end
				Options.Pls:SetValues(Ps)
			end)
		end
    })	
	local Dropdown = Tabs.Cash:AddDropdown("Pls", {
		Title = "Player for transfer",
		Values = Ps,
		Multi = false,
		--Default = 1,
	})
	--Dropdown:SetValue(Banker)
	Dropdown:OnChanged(function(pl_index)
		--Banker = Value
		Options.Pls.Close()
		if pl_index and exper then
			local pl_index = tostring(pl_index)
			if pl_index and not Options[pl_index] then
				local Input = Tabs.Cash:AddInput("Input"..pl_index, {
					Title = pl_index,
					Default = "",
					--Placeholder = "Placeholder",
					Numeric = true,
					Finished = false,
				})
				
				local Toggle = Tabs.Cash:AddToggle(pl_index, {Title = "Start transfer", Default = false})
				Toggle:OnChanged(function()
					spawn(function()
						if Options[pl_index].Value then
							--trans_cash = true
							if not table.find(Bankers, pl_index) then
								table.insert(Bankers, pl_index)
							end
						else
							if table.find(Bankers, pl_index) then
								table.remove(Bankers, table.find(Bankers, pl_index))
							end
						end
						Ps = {}
						for i,v in next, Players:GetPlayers() do
							table.insert(Ps, v.Name)
						end
						Options.Pls:SetValues(Ps)
					end)
					
					if Options[pl_index].Value then
						spawn(function()
							--local _,err=pcall(function()
							local first_start = true
							while Options[pl_index].Value and wait(1) do
								local tmp = {}
								for _,v in next, tmp_Bankers do
									if v[1] == pl_index then
										if first_start then
											first_start = false
											if Options["Input"..pl_index].Value ~= v[2] then
												v[2] = tonumber(Options["Input"..pl_index].Value)
											end
										end
										Options["Input"..pl_index]:SetValue(v[2])
										if v[2] <= 0 then
											wait(2)
											Options[pl_index].Destroy()
											Options["Input"..pl_index].Destroy()
											if table.find(Bankers, pl_index) then
												table.remove(Bankers, table.find(Bankers, pl_index))
											end
											kassa[pl_index] = nil
											for num = 1, #info_bank do
												local tmp = string.split(info_bank[num], " ")
												if tmp[1] == pl_index then
													if tonumber(tmp[3]) ~= 0 then
														info_bank[num] = tmp[1].." "..tmp[2].." ".."0"
														break
													end
												end
											end
											local _,err=pcall(function()
												local tmp = ""
												for _,vvvv in info_bank do
													tmp = tmp..vvvv.."\n"
												end
												writefile("transfer.txt", tmp)
											end)if err then con_log(err, "ERROR write file:")end
										else
											table.insert(tmp, {v[1], v[2]})
										end
									else
										table.insert(tmp, {v[1], v[2]})
									end
								end
								tmp_Bankers = tmp
							end
							--end)if err then con_log(err)end
						end)
					end
				end)
			else
				Options[pl_index].Destroy()
				Options["Input"..pl_index].Destroy()
				if table.find(Bankers, pl_index) then
					table.remove(Bankers, table.find(Bankers, pl_index))
				end
				kassa[pl_index] = nil
			end
			spawn(function()
				Options.Pls.Value = ""
			end)
		end
	end)
	
	
	local infos
    local tmp_file = ""
	pcall(function()
		tmp_file = readfile("transfer.txt")
		info_bank = string.split(tmp_file, "\n")
		if #string.split(info_bank[1], " ") < 3 then
			info_bank = {}
		end
	end)
	Tabs.Stat:AddButton({
        Title = "Reload data",
        --Description = "",
        Callback = function()
			spawn(function()
				local _,err=pcall(function()
					tmp_file = readfile("transfer.txt")
					--infos:SetTitle(tmp_file)
					if infos then
						infos.Destroy()
						infos = Tabs.Stat:AddParagraph({
							Title = "Paragraph",
							Content = tmp_file
						})
					end
				end)if err then con_log(err, "error data:")end
			end)
		end
	})
	
	infos = Tabs.Stat:AddParagraph({
        Title = "Paragraph",
        Content = tmp_file
	})
	
	
	
	--local Input = Tabs.Stat:AddInput("d", {
		--Title = tmp_file,
		--Default = "",
		--Placeholder = "Placeholder",
		--Numeric = false,
		--Finished = false,
	--})
	
    --local Toggle = Tabs.Main:AddToggle("Trans", {Title = "Transfer money", Default = false})
	--Toggle:OnChanged(function()
        --local flag = Options.Trans.Value
		--if flag then
			--spawn(function()
			--end)
		--end
    --end)
	



end
SaveManager:SetLibrary(Fluent)
InterfaceManager:SetLibrary(Fluent)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes({})
InterfaceManager:SetFolder("FluentScriptHub")
SaveManager:SetFolder("FluentScriptHub/specific-game")

--InterfaceManager:BuildInterfaceSection(Tabs.Settings)
--SaveManager:BuildConfigSection(Tabs.Settings)


Window:SelectTab(3)

Fluent:Notify({
    Title = "AdoptMe started",
    Content = "The script has been loaded.",
    Duration = 5
})

SaveManager:LoadAutoloadConfig()

_,err=pcall(function()
game:GetService("CoreGui").ScreenGui:GetChildren()[2].Position = UDim2.new(0,50,0,50)
--Window.Position = UDim2.new(0.25, 0, 0.5, 0)
--Window.Position = Vector2.new(50,50)
--con_log(Window.Position)
end)
if err then
con_log(err)
end


local event_flag
local ev_ev1 = true
local ev_ev_2 = false

coroutine.wrap(
function()
	local hint, last_c
	local count = 0
	local work_time = 0
	local work_point = os.time()
	
	
	while not Fluent.Unloaded do
		wait(.05)
		pcall(function()
			
			
			if Player.PlayerGui.DailyLoginApp.Frame.Visible then
				local _,err=pcall(function()
					if Player.PlayerGui.DailyLoginApp.Frame.Body.Buttons.ClaimButton.Face.TextLabel.Text ~= "CLOSE" then
						getconnections(Player.PlayerGui.DailyLoginApp.Frame.Body.Buttons.ClaimButton.MouseButton1Down)[1]:Fire()
						wait(.1)
						getconnections(Player.PlayerGui.DailyLoginApp.Frame.Body.Buttons.ClaimButton.MouseButton1Up)[1]:Fire()
						ReplicatedStorage.API:FindFirstChild("DailyLoginAPI/ClaimDailyReward"):InvokeServer()
						getconnections(Player.PlayerGui.DailyLoginApp.Frame.Body.Buttons.ClaimButton.MouseButton1Click)[1].Function()
						wait(.3)
					end
					getconnections(Player.PlayerGui.DailyLoginApp.Frame.Body.Buttons.ClaimButton.MouseButton1Down)[1]:Fire()
					getconnections(Player.PlayerGui.DailyLoginApp.Frame.Body.Buttons.ClaimButton.MouseButton1Up)[1]:Fire()
					getconnections(Player.PlayerGui.DailyLoginApp.Frame.Body.Buttons.ClaimButton.MouseButton1Click)[1].Function()
				end)
				if err then
					con_log(err, "error daily rewards")
					pcall(function()
					set_thread_identity(2)
					require(ReplicatedStorage.Fsys).load("UIManager").set_app_visibility("DailyLoginApp", false)
					set_thread_identity(7)
					end)
				end			
			end
			if Player.PlayerGui.PlaytimePayoutsApp.Enabled then
				local _,err=pcall(function()
					local _,err=pcall(function()
						getconnections(Player.PlayerGui.PlaytimePayoutsApp:FindFirstChild("DontShow",true).Activated)[1]:Fire()
						wait(.3)
					end)
					getconnections(Player.PlayerGui.PlaytimePayoutsApp:FindFirstChild("DepthButton",true).MouseButton1Down)[1]:Fire()
					--game:GetService("ReplicatedStorage").API:FindFirstChild("PayAPI/Collect"):FireServer()
					wait(.3)
					getconnections(Player.PlayerGui.PlaytimePayoutsApp:FindFirstChild("DepthButton",true).MouseButton1Up)[1]:Fire()
					getconnections(Player.PlayerGui.PlaytimePayoutsApp:FindFirstChild("DepthButton",true).MouseButton1Click)[1].Function()
				end)
				if err then
					pcall(function()
					set_thread_identity(2)
					require(ReplicatedStorage.Fsys).load("UIManager").set_app_visibility("PlaytimePayoutsApp", false)
					set_thread_identity(7)
					end)
				end			
			end
			
			if Player.PlayerGui.DialogApp.Dialog.NormalDialog.Visible then
				if Player.PlayerGui.DialogApp.Dialog.NormalDialog.Info.TextLabel.Text:find("!") then
					if #Player.PlayerGui.DialogApp.Dialog.NormalDialog.Buttons:GetChildren() > 2 then
						set_thread_identity(2)
						require(ReplicatedStorage.Fsys).load("UIManager").set_app_visibility("DialogApp", false)
						set_thread_identity(7)
					end
				end
				--if Player.PlayerGui.DialogApp.Dialog.NormalDialog.Buttons.ButtonTemplate.Face.TextLabel.Text == 'Okay' then
				if #Player.PlayerGui.DialogApp.Dialog.NormalDialog.Buttons:GetChildren() < 3 then
					getconnections(Player.PlayerGui.DialogApp.Dialog.NormalDialog.Buttons.ButtonTemplate.MouseButton1Down)[1].Function()
					getconnections(Player.PlayerGui.DialogApp.Dialog.NormalDialog.Buttons.ButtonTemplate.MouseButton1Up)[1].Function()
					getconnections(Player.PlayerGui.DialogApp.Dialog.NormalDialog.Buttons.ButtonTemplate.MouseButton1Click)[1].Function()
				end
			end
			if Player.PlayerGui.MinigameRewardsApp.Body.Visible then
				getconnections(Player.PlayerGui.MinigameRewardsApp.Body.Button.MouseButton1Down)[1].Function()
				getconnections(Player.PlayerGui.MinigameRewardsApp.Body.Button.MouseButton1Down)[1]:Fire()
				getconnections(Player.PlayerGui.MinigameRewardsApp.Body.Button.MouseButton1Up)[1].Function()
				--getconnections(Player.PlayerGui.MinigameRewardsApp.Body.Button.MouseButton1Up)[1]:Fire()
				getconnections(Player.PlayerGui.MinigameRewardsApp.Body.Button.MouseButton1Click)[1].Function()
				--getconnections(Player.PlayerGui.MinigameRewardsApp.Body.Button.MouseButton1Click)[1]:Fire()
			end
			if Player.PlayerGui.DialogApp.Dialog.ItemPreviewDialog.Visible then
				getconnections(Player.PlayerGui.DialogApp.Dialog.ExitButton.MouseButton1Down)[1]:Fire()
				wait(.1)
				getconnections(Player.PlayerGui.DialogApp.Dialog.ExitButton.MouseButton1Up)[1]:Fire()
				getconnections(Player.PlayerGui.DialogApp.Dialog.ExitButton.MouseButton1Click)[1].Function()
				ReplicatedStorage.API["HousingAPI/ClaimAllDeliveries"]:FireServer()
				if PetID then
					wait(1)
					ReplicatedStorage.API["ToolAPI/Equip"]:InvokeServer(PetID)
				end
			end
			--[==[
			local tmp=require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].social_stones_2025
			if tmp then
				if tmp >= 25 then
					ReplicatedStorage.API["SocialStonesAPI/AttemptExchange"]:FireServer("food", "butterfly_2025_snapdragon_flower", tmp//25)
					wait(1)
					for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.food do
						if v.id == "butterfly_2025_snapdragon_flower" then
							ReplicatedStorage.API["LootBoxAPI/ExchangeItemForReward"]:InvokeServer(v.id, i)
						end
					end
				end
			end
			--]==]
		end)
		
		--[===[
		if Options.Event1.Value and not busy then
		
			--if (os.date("!*t").min-1) % 10 == 0 or ((os.date("!*t").min-2) % 10 == 0 and os.date("!*t").sec <= 40) then
			if os.date("!*t").min == 11 or (os.date("!*t").min == 12 and os.date("!*t").sec <= 40) or
			os.date("!*t").min == 31 or (os.date("!*t").min == 32 and os.date("!*t").sec <= 40) or
			os.date("!*t").min == 51 or (os.date("!*t").min == 52 and os.date("!*t").sec <= 40) then
				local _,err=pcall(function()
					event_flag = true
					busy = true
					if Workspace.Interiors:FindFirstChildWhichIsA("Model") then
						if not Workspace.Interiors:FindFirstChildWhichIsA("Model").Name:find("MainMap") then
							GoToStore("MainMap")
						end
					else
						GoToStore("MainMap")
					end
					Player.Character.HumanoidRootPart.CFrame = CFrame.new(-590, 35, -1670)
					local t = os.time()
					local flag = true
					while farm do
						_,chk=pcall(function()
							local tmp = Player.PlayerGui.MinigameHotbarApp.Hotbar.SwordButton
						end)
						if not chk then
							break 
						end
						if os.time()-t > 150 then
							flag = false
							break
						end
						wait(.1)
					end
					if flag then
						wait(5)
						con_log(Player.Character.HumanoidRootPart.Position)---
						if Player.Character.HumanoidRootPart.Position.X > 0 then
							Player.Character.HumanoidRootPart.CFrame = CFrame.new(12023, 9899, 5989)
						else
							if Player.Character.HumanoidRootPart.Position.X < -9500 then
								--Player.Character.HumanoidRootPart.CFrame = CFrame.new(-12023, 9899, 8989)
								Player.Character.HumanoidRootPart.CFrame = CFrame.new(-11977, 9899, 8989)
							elseif Player.Character.HumanoidRootPart.Position.X < -6500 then
								Player.Character.HumanoidRootPart.CFrame = CFrame.new(-8977, 9899, 8989)
							elseif Player.Character.HumanoidRootPart.Position.X < -3500 then
								Player.Character.HumanoidRootPart.CFrame = CFrame.new(-5977, 9899, 8989)
							else
								Player.Character.HumanoidRootPart.CFrame = CFrame.new(-2977, 9899, 8989)
							end
						end
						Workspace.Pstone.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,-3,0))
						local _,chk=pcall(function()
						local t=os.time()
							while os.time()-t < 120 do
								getconnections(Player.PlayerGui.MinigameHotbarApp.Hotbar.SwordButton.Button.MouseButton1Down)[1].Function()
								getconnections(Player.PlayerGui.MinigameHotbarApp.Hotbar.SwordButton.Button.MouseButton1Down)[1]:Fire()
								wait(.5)
							end
						end)
						wait(30)
						Workspace.Pstone.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,-3,0))
					end
					con_log("", "event done")
					busy = false
					event_flag = false
				end)
				if err then
					con_log(err)
					event_flag = false
					busy = false
				end
			--else
				--event_flag = false
			end
		
		end
		--]===]
		
		local _,err=pcall(function()
			if farm then
				work_time = work_time + (os.time() - work_point)
				work_point = os.time()
				--Player.PlayerGui.indicator.Container.Amount.Text = tostring(count).."\n"..tostring(work_time)
				local tmp = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].equip_manager.food[1]
				if tmp then
					if tmp.id == "pet_age_potion" then
						pcall(function()
							ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(tmp.unique)
						end)
					end
				end
			end
			hint = Player.PlayerGui.HintApp.TextLabel.Text
			if hint ~= last_c then
				last_c = hint
				hint = hint:split("(")[2]:split(" ")[1]:split("+")[2]
				if hint then
					if hint ~= "50" and hint ~= "100" then
						count = count + tonumber(hint)
						if work_time > 86400 then
							Player.PlayerGui.indicator.Container.Amount.Text = tostring(count).."\n"..tostring(work_time//86400%86400).." d "..tostring(work_time//3600%3600)..":"..tostring(work_time//60%60)
						else
							Player.PlayerGui.indicator.Container.Amount.Text = tostring(count).."\n"..tostring(work_time//3600%3600)..":"..tostring(work_time//60%60)
						end
						--wait(2)
						--Player.PlayerGui.BucksIndicatorApp.CurrencyIndicator.Container.Amount.Text = Player.PlayerGui.BucksIndicatorApp.CurrencyIndicator.Container.Amount.Text.."\n("..tostring(count)..")"
					end
				end
			end
		end)--if err then con_log(err)end
		
	end
end)()




coroutine.wrap(
function()
	local timer_b = 0
	local redstr_flag
	local bal
	local wfile_flag = true
	
	local main_pets = {}
	local send_for_egg = {}
	
	while not Fluent.Unloaded do
		wait(1)
		local _,err=pcall(function()
			
			set_lure()
			
			pcall(function()
				if Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA("Model") then
					if not Player.PlayerGui.BucksIndicatorApp.Enabled then
						Player.PlayerGui.BucksIndicatorApp.Enabled = true
					end
					if not Player.PlayerGui.AltCurrencyIndicatorApp.Enabled then
						Player.PlayerGui.AltCurrencyIndicatorApp.Enabled = true
					end
				end
			end)
			--[=[
			pcall(function()
				if event_flag then
					con_log("ginger time!")----
					for i,v in ReplicatedStorage.Resources.IceSkating.GingerbreadMarkers:GetChildren() do
						ReplicatedStorage.API["WinterEventAPI/PickUpGingerbread"]:InvokeServer(v.Name)
					end
					ReplicatedStorage.API["WinterEventAPI/RedeemPendingGingerbread"]:FireServer()
					event_flag = false
				end
			end)
			--]=]
			
			if is_manager then
				if bal ~= require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].money then
					bal = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].money
					--writefile("balances.txt", tostring(bal))
				end
			elseif wfile_flag then
				--writefile("balances.txt", "-1")
				wfile_flag = false
			end
			
			if farm and not busy and not event_flag then
				busy = true
				local check_sell_house
				local _,err=pcall(function()
					for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].house_manager do
						if v.name == "My Christmas Pudding House" then
							ReplicatedStorage.API["HousingAPI/SellHouse"]:InvokeServer(v.house_id)
							check_sell_house = true
						end
					end
				end)
				local _,err=pcall(function()
					if GetCurrentPetOut().goal_state.distance_to_owner > 50 or tostring(GetCurrentPetOut().goal_state.distance_to_owner) == "nan" then
						con_log(GetCurrentPetOut().goal_state.distance_to_owner)-------
						ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(PetID)
						ReplicatedStorage.API["ToolAPI/Equip"]:InvokeServer(PetID)
						wait(1)
					end
				end)
				if err then
					con_log(err)------
					ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(PetID)
					ReplicatedStorage.API["ToolAPI/Equip"]:InvokeServer(PetID)
					wait(1)
				end
				if GetCurrentPetOut() then
					local tmpID = string.split(GetCurrentPetOut().unique_id, "-")
					tmpID = table.concat(tmpID, "-", 1, #tmpID-1)
					if PetID ~= tmpID then
						ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(PetID)
						ReplicatedStorage.API["ToolAPI/Equip"]:InvokeServer(tmpID)
						PetID = tmpID
					end
				else
					Pets()
				end
				
				if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].char_wrapper_raw.location.destination_id == "MainMap" then
					--[[
					if not require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.baby_ailments["summerfest_bonfire"] then
						--con_log(require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID])----
						if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID] then
							if not require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID]["summerfest_bonfire"] then
								--con_log("","TP1")----
								Workspace.Pstone.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,-3,0))
								if ev_ev1 then
									wait(10)
								end
								if ev_ev_2 then
									wait(5)
								end
								GoToHome()
								wait(2)
								Player.Character.HumanoidRootPart.CFrame = Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA('Model'):GetModelCFrame():ToWorldSpace(CFrame.new(-7, 100, 3))
								Workspace.Pstone.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,-3,0))
							end
						else
							--con_log("","TP2")----
							Workspace.Pstone.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,-3,0))
							if ev_ev1 then
								wait(10)
							end
							if ev_ev_2 then
								wait(5)
							end
							GoToHome()
							wait(2)
							Player.Character.HumanoidRootPart.CFrame = Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA('Model'):GetModelCFrame():ToWorldSpace(CFrame.new(-7, 100, 3))
							Workspace.Pstone.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,-3,0))
						end
					end
					--]]
				end
				
				if check_sell_house or (require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].char_wrapper_raw.location.destination_id == "Neighborhood" and
				require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].house_interior.furniture) then
					--if not busy then
						busy = true
						wait(15)
						local _,err=pcall(function()
							con_log("babyfarm")----
							ReplicatedStorage.API["TeamAPI/ChooseTeam"]:InvokeServer("Babies", {},true)
							wait(2)
							local n = 0
							while n < 20 do
								wait(1)
								n = n + 1
								if Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA("Model") and Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA("Model").Name == Player.Name then
									break
								end
							end
							bed = Furniture(Beds)
							wash = Furniture(Washings)
							toil = Furniture({"Toilet"})
							f_bowl = Furniture(bowls_f)
							w_bowl = Furniture(bowls_w)
							wait(2)
							Player.Character.HumanoidRootPart.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,100,0))
							Workspace.Pstone.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,-3,0))
						end)
						--busy = false
					--end
				end
				
				
				if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets[PetID].properties.age == 6 or 
				require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets[PetID].id == "practice_dog" then
					if Options.hatch_egg.Value then
						if Options.with_bottle.Value and require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets[PetID].properties.xp >= 2400 then
						--con_log("","ПИДАРАСИНА")
						else
							local chk_work_ailm
							local not_hide
							for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID] or {} do
								if i:find("mystery") or table.find(red_task, i) or table.find(wend_task, i) or table.find(care_task, i) then
									chk_work_ailm = true
								end
							end
							if not chk_work_ailm then
								chk_work_ailm = true
								local egg_uniq
								for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets do
									if v.id == "practice_dog" or v.id:find("minigame") then
									elseif endswith(v.id, "_egg") then
										if v.id == Options.eggs.Value then
											egg_uniq = v.unique
										end
									else
										if v.properties.age < 6 or v.properties.xp >= 2400 then
											if table.find(worked_pets, v.id) then
												chk_work_ailm = false
												if v.properties.xp >= 2400 and PetID == v.unique then
													not_hide = true
												end
											end
										end
										--if Options.with_bottle.Value and v.properties.xp > 2400 then--and table.find(worked_pets, v.id) then
											--chk_work_ailm = false
										--end
									end
								end
								if chk_work_ailm and Options.hatch_egg.Value then
									--require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].money
									if not egg_uniq then
										if Options.eggs.Value == "retired_egg" and not VIP then
											local res = ""
											local _,err=pcall(function()
												res = game:HttpGet("http://"..visor.."/old_egg?"..Player.Name)
											end)
											if res ~= "" then
												if not send_for_egg[res] then
													send_for_egg[res] = {os.time()-120, 600}
												elseif send_for_egg[res][1] + 120 < os.time() then
													send_for_egg[res][1] = os.time()
													if send_for_egg[res][2] == 0 then
														send_for_egg[res][2] = 600
													end
												end
											end
										else
											if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].money >= egg_price[Options.eggs.Value] then
												local buy = ReplicatedStorage.API["ShopAPI/BuyItem"]:InvokeServer("pets", Options.eggs.Value, {})
												if buy == "success" then
													wait(1)
													PetID = nil
													Pets()
												else
													con_log(buy, "wrong buy egg:")
												end
											end
										end
									else
										ReplicatedStorage.API["ToolAPI/Equip"]:InvokeServer(egg_uniq)
										wait(1)
										PetID = nil
										Pets()
										local n = 0
										for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets do
											if v.id == "retired_egg" then
												n = n + 1
											end
										end
										if n > 1 then
											for res,t in send_for_egg or {} do
												if t[2] < 1000 then
													send_for_egg[res][2] = send_for_egg[res][2] + (n-1)*600
												end
											end
										end
									end
								end
							end
							if not chk_work_ailm and not not_hide then
								ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(PetID)
								wait(1)
								PetID = nil
								neon_enabled = not Options.with_bottle.Value
								Pets()
							end
						end
					end
				elseif Options.with_bottle.Value and not endswith(require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets[PetID].id, "_egg") then
					local Potions = {}
					local p1
					local n = 0
					for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.food do
						if v.id == "pet_age_potion" then
							n = n + 1
							if n > 6 then
								p1 = v.unique
								break
							end
							table.insert(Potions, v.unique)
						end
					end
					if p1 then
						ReplicatedStorage:FindFirstChild("PetObjectAPI/CreatePetObject",true):InvokeServer("__Enum_PetObjectCreatorType_2", {
						["additional_consume_uniques"] = {unpack(Potions)},
						["pet_unique"] = PetID,
						["unique_id"] = p1})
						local t = os.time()
						while require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets[PetID].properties.age ~= 6 and farm and os.time()-t < 15 do
							wait(.1)
						end
						con_log("sec.", os.time()-t)
					end
				end
				
				if Options.hatch_egg.Value then
					local _,err=pcall(function()
						for res,t in send_for_egg or {} do
							if t[1]+120 <= os.time() and t[2] > 0 then
								if Players:FindFirstChild(res) then
									if pcall(function() local tmp = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[res].char_wrapper_raw.location end) then
										GoToHome(res)
										for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].house_interior.furniture do
											if v.id == "mannequins_2024_mannequin" then
												if v.price == 100 then
													for _=1,3 do
														pcall(function()
															ReplicatedStorage.API["AvatarAPI/DeleteOutfit"]:FireServer("player", v.outfit_name)
														end)
														local _,err_ch=pcall(function()
															buy = ReplicatedStorage.API["AvatarAPI/BuyMannequinOutfit"]:InvokeServer(Players[res], i, v.outfit_version, 100)
															send_for_egg[res][2] = send_for_egg[res][2] - 100
															wait()
															ReplicatedStorage.API["AvatarAPI/DeleteOutfit"]:FireServer("player", v.outfit_name)
														end)if err_ch then con_log(err_ch)end
														wait(.3)
													end
													break
												end
											end
										end
										GoToHome()
										send_for_egg[res][1] = os.time()
										if send_for_egg[res][2] < 100 then
											send_for_egg = {}
										end
									end
								end
							end
						end
					end)if err then con_log(err) end
					local _,err=pcall(function()
						if Player.PlayerGui.DialogApp.Dialog.NormalDialog.Visible or Player.PlayerGui.DialogApp.Dialog.HeaderDialog.Visible then
							local dialog = "NormalDialog"
							if Player.PlayerGui.DialogApp.Dialog.HeaderDialog.Visible then
								dialog = "HeaderDialog"
							end
							for i,v in Players:GetPlayers() do
								if v ~= Player and Player.PlayerGui.DialogApp.Dialog[dialog].Info.TextLabel.Text:find(v.Name) then
									con_log(v.Name, "found request:")
									local _,err=pcall(function()
										getconnections(Player.PlayerGui.DialogApp.Dialog[dialog].Buttons:GetChildren()[#Player.PlayerGui.DialogApp.Dialog[dialog].Buttons:GetChildren()].MouseButton1Down)[1]:Fire()
										wait(.1)
										getconnections(Player.PlayerGui.DialogApp.Dialog[dialog].Buttons:GetChildren()[#Player.PlayerGui.DialogApp.Dialog[dialog].Buttons:GetChildren()].MouseButton1Up)[1]:Fire()
										getconnections(Player.PlayerGui.DialogApp.Dialog[dialog].Buttons:GetChildren()[#Player.PlayerGui.DialogApp.Dialog[dialog].Buttons:GetChildren()].MouseButton1Click)[1].Function()
									end)
									if err then
										ReplicatedStorage.API["TradeAPI/AcceptOrDeclineTradeRequest"]:InvokeServer(v, true)
									end
									
									wait(5)
									local t = os.time()
									while not Player.PlayerGui.TradeApp.Frame.NegotiationFrame.Visible and os.time()-t < 10 do
										wait(.3)
									end
									local t = os.time()
									while Player.PlayerGui.TradeApp.Frame.NegotiationFrame.Body.Accept.Face.TextLabel.Text ~= "Accept" and os.time()-t < 20 do
										wait(.3)
									end
									ReplicatedStorage.API["TradeAPI/AcceptNegotiation"]:FireServer()
									
									local t = os.time()
									while not Player.PlayerGui.TradeApp.Frame.ConfirmationFrame.Visible and os.time()-t < 20 do
										wait(.3)
									end
									wait(1)
									ReplicatedStorage.API["TradeAPI/ConfirmTrade"]:FireServer()
									if not pcall(function() local tmp=Player.PlayerGui.TradeApp.Frame.NegotiationFrame.Body.PartnerOffer.Slots.Slot1.ItemImageTemplate end) then
										pcall(function()
											getconnections(Player.PlayerGui.TradeApp.Frame.NegotiationFrame.Body.Decline.MouseButton1Down)[1]:Fire()
											wait(.1)
											getconnections(Player.PlayerGui.TradeApp.Frame.NegotiationFrame.Body.Decline.MouseButton1Up)[1]:Fire()
											getconnections(Player.PlayerGui.TradeApp.Frame.NegotiationFrame.Body.Decline.MouseButton1Click)[1].Function()
										end)
									end
									break
								end
							end
						end

						local _,err=pcall(function()
							local Pts = {}
							for uniq, v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets do
								--if v.properties.age == 6 and v.properties.neon and not v.properties.mega_neon then
								if v.properties.age == 6 and not v.properties.neon and not v.properties.mega_neon and v.properties.friendship_level < 10 then
									if not Pts[v.id] then
										Pts[v.id] = {}
									end
									table.insert(Pts[v.id], v.unique)
								end
							end
							for i, v in Pts do
								if #v < 4 then
								else
									local Spec={}
									for n=1, #v//4 do
										local tmp = (n-1)*4
										local res = ReplicatedStorage.API["PetAPI/DoNeonFusion"]:InvokeServer({v[tmp+1], v[tmp+2], v[tmp+3], v[tmp+4]})
										if res then
											con_log(i, "Create neon:")
										end
									end
								end
							end
						end)if err then con_log(err, "Error make Neon:")end
						local _,err=pcall(function()
							local Pts = {}
							for uniq, v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets do
								if v.properties.age == 6 and v.properties.neon and not v.properties.mega_neon then
									if not Pts[v.id] then
										Pts[v.id] = {}
									end
									table.insert(Pts[v.id], v.unique)
								end
							end
							for i, v in Pts do
								if #v < 4 then
								else
									local Spec={}
									for n=1, #v//4 do
										local tmp = (n-1)*4
										local res = ReplicatedStorage.API["PetAPI/DoNeonFusion"]:InvokeServer({v[tmp+1], v[tmp+2], v[tmp+3], v[tmp+4]})
										if res then
											con_log(i, "Create mega-neon:")
										end
									end
								end
							end
						end)if err then con_log(err, "Error make Mega-Neon:")end					
						
						if visor then
							if visor ~= "" and visor:find(":") then
								local all_pets = {}
								for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets do
									if v.id == "practice_dog" or v.id:find("minigame") or v.properties.friendship_level > 10 and endswith(v.id, "_egg") then
									elseif table.find(worked_pets, v.id) then
										if not all_pets[v.id] then
											all_pets[v.id] = 0
										end
										local n = 1
										if v.properties.neon then
											n = 4
										elseif v.properties.mega_neon then
											n = 16
										end
										all_pets[v.id] = all_pets[v.id] + n
									end
								end
								local req = {}
								for i,v in all_pets do
									table.insert(req, i.."="..tostring(v))
								end
								
								local _,err=pcall(function()
									local res = game:HttpGet("http://"..visor.."/"..Player.Name.."?"..table.concat(req, "&"))
									if VIP then
										local res = game:HttpGet("http://"..visor.."/VIP?"..Player.Name)
										if Players:FindFirstChild(res) then
											if pcall(function() local tmp = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[res].char_wrapper_raw.location end) then
												local unq
												while not unq do
													for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets do
														if v.id == "retired_egg" then
															unq = v.unique
															break
														end
													end
													if not unq then
														if ReplicatedStorage.API["ShopAPI/BuyItem"]:InvokeServer("pets", "retired_egg", {}) ~= "success" then
															break
														end
													end
												end
												ReplicatedStorage.API["TradeAPI/GiveItem"]:InvokeServer(Players[res], unq)
											end
										end
									end
								end)
								if err then
									con_log(err, "VISOR error:")
								end
								
								local legends = {}
								for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets do
									if v.id == "practice_dog" or v.id:find("minigame") or v.properties.friendship_level > 10 or v.unique == PetID then
									elseif endswith(v.id, "_egg") and not table.find(eggs, v.id) then
									elseif v.properties.age == 6 then
										if not Options.with_bottle.Value or v.properties.xp < 2400 then
											if table.find(legendary_pets, v.id) then--and not table.find(main_pets, v.id) then
												table.insert(legends, v.unique)
											elseif Player.Name == "Trulala09348" and v.id == "winter_2023_beluga_whale" then
											else--if not table.find(main_pets, v.id) then
												local res = ""
												local _,err=pcall(function()
													res = game:HttpGet("http://"..visor.."/"..v.id)
												end)
												if err then
													con_log(err, "VISOR error:")
												end
												if res ~= "" then
													local _,err=pcall(function()
														if res == Player.Name then
															--table.insert(main_pets, v.id)
														elseif Players:FindFirstChild(res) then
															if pcall(function() local tmp = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[res].char_wrapper_raw.location end) then
																ReplicatedStorage.API["TradeAPI/GiveItem"]:InvokeServer(Players[res], v.unique)
															end
														end
													end)if err then con_log(err) end
												end
											end
										end
									end
								end
								if #legends > 0 then
									if not Player.PlayerGui.TradeApp.Frame.Visible then
										local res = ""
										local _,err=pcall(function()
											res = game:HttpGet("http://"..visor.."/"..require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets[legends[1]].id)
										end)
										if err then
											con_log(err, "VISOR error:")
										end
										if res ~= "" then
											local _,err=pcall(function()
												if res == Player.Name then
													--table.insert(main_pets, require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets[legends[1]].id)
													res = ""
												end
											end)if err then con_log(err) end
										end
										if res ~= "" and Players:FindFirstChild(res) then
											if pcall(function() local tmp = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[res].char_wrapper_raw.location end) then
												ReplicatedStorage.API["TradeAPI/SendTradeRequest"]:FireServer(Players[res])
												wait(3)
												local n = 0
												while not Player.PlayerGui.TradeApp.Frame.Visible do
													con_log("wait for tradeApp...", res)
													wait(2)
													n = n + 2
													if n > 40 then
														break
													end
												end
												if n <= 40 then
													ReplicatedStorage.API["TradeAPI/AddItemToOffer"]:FireServer(legends[1])
													wait(5)
													ReplicatedStorage.API["TradeAPI/AcceptNegotiation"]:FireServer()
													wait(5)
													--while Player.PlayerGui.TradeApp.Frame.NegotiationFrame.Body.Accept.Face.TextLabel.Text ~= "Accept (1)" and os.time()-t < 20 do
													local t = os.time()
													while not Player.PlayerGui.TradeApp.Frame.ConfirmationFrame.Visible and os.time()-t < 20 do
														wait(.3)
													end
													wait(1)
													ReplicatedStorage.API["TradeAPI/ConfirmTrade"]:FireServer()
													
													local t = os.time()
													while Player.PlayerGui.TradeApp.Frame.Visible and os.time()-t < 30 do
														wait(1)
														if Player.PlayerGui.DialogApp.Dialog.NormalDialog.Visible then
															pcall(function()
																getconnections(Player.PlayerGui.DialogApp.Dialog.NormalDialog.Buttons.ButtonTemplate.MouseButton1Down)[1]:Fire()
																wait(.1)
																getconnections(Player.PlayerGui.DialogApp.Dialog.NormalDialog.Buttons.ButtonTemplate.MouseButton1Up)[1]:Fire()
																getconnections(Player.PlayerGui.DialogApp.Dialog.NormalDialog.Buttons.ButtonTemplate.MouseButton1Click)[1].Function()
															end)
														end
													end
												end
											end
										end
									end
								end
							end
						end
						
						
						---[[
						if t_user ~= "" then
							if Players:FindFirstChild(t_user) then
								if pcall(function() local tmp = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[t_user].char_wrapper_raw.location end) then
									local legends = {}
									for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pets do
										if v.id == "practice_dog" or v.id:find("minigame") or v.properties.friendship_level > 10 then
										elseif endswith(v.id, "_egg") and not table.find(eggs, v.id) then
											table.insert(legends, v.unique)
										elseif v.properties.mega_neon then
											if table.find(legendary_pets, v.id) then
												table.insert(legends, v.unique)
											elseif Player.Name == "Trulala09348" and v.id == "winter_2023_beluga_whale" then
											else
												ReplicatedStorage.API["TradeAPI/GiveItem"]:InvokeServer(Players[t_user], v.unique)
											end
										elseif v.properties.age == 6 and v.properties.neon then
											if table.find(legendary_pets, v.id) then
												table.insert(legends, v.unique)
											end
										end
									end
									
									if #legends < 18 and Options.give_items.Value then
										for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.gifts do
											if not v.id:find("furniture") and not v.id:find("funiture") then
												table.insert(legends, v.unique)
												--con_log(v.id)
											end
										end
										for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.toys do
											if v.id ~= "trade_license" and not v.id:find("default") then
												table.insert(legends, v.unique)
												--con_log(v.id)
											end
										end
										for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.transport do
											if v.id ~= "ice_skates" then
												table.insert(legends, v.unique)
												--con_log(v.id)
											end
										end
										for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.strollers do
											if not v.id:find("default") then
												table.insert(legends, v.unique)
												--con_log(v.id)
											end
										end
										for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.stickers do
											if table.find({"ice_dimension_2025_icy_porcupine_sticker",
											"ice_dimension_2025_chilly_penguin_sticker",
											"ice_dimension_2025_frostbite_bear_and_cub_animated_sticker",
											"ice_dimension_2025_snowy_mammoth_sticker",
											"ice_dimension_2025_frostbite_bear_sticker"}, v.id) then
												table.insert(legends, v.unique)
												--con_log(v.id)
											end
										end
										for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.pet_accessories do
											if not table.find({"white_bowtie", "cowbell", "blue_cap"}, v.id) then
												table.insert(legends, v.unique)
												--con_log(v.id)
											end
										end
										for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.food do
											if v.id:find("potion") then
												if not v.id:find("pet_age_potion") then
													table.insert(legends, v.unique)
													--con_log(v.id)
												end
											elseif v.id == "winter_2025_yarn_beanie_bait" then
													--con_log(v.id)
												table.insert(legends, v.unique)
											end
										end
									end
									
									if #legends > 0 then
										if not Player.PlayerGui.TradeApp.Frame.Visible then
											ReplicatedStorage.API["TradeAPI/SendTradeRequest"]:FireServer(Players[t_user])
											wait(3)
											local n = 0
											while not Player.PlayerGui.TradeApp.Frame.Visible do
												con_log("wait for tradeApp...")
												wait(2)
												n = n + 2
												if n > 10 then
													break
												end
											end
											local n_item
											if n <= 10 then
												for i,v in legends do
													ReplicatedStorage.API["TradeAPI/AddItemToOffer"]:FireServer(v)
													n_item = i
													if i == 18 then
														break
													end
												end
												wait(5)
												ReplicatedStorage.API["TradeAPI/AcceptNegotiation"]:FireServer()
												wait(5)
												--while Player.PlayerGui.TradeApp.Frame.NegotiationFrame.Body.Accept.Face.TextLabel.Text ~= "Accept (1)" and os.time()-t < 20 do
												local t = os.time()
												while not Player.PlayerGui.TradeApp.Frame.ConfirmationFrame.Visible and os.time()-t < 20 do
													wait(.3)
												end
												wait(1)
												ReplicatedStorage.API["TradeAPI/ConfirmTrade"]:FireServer()
												
												local t = os.time()
												while Player.PlayerGui.TradeApp.Frame.Visible and os.time()-t < 30 do
													wait(1)
													if Player.PlayerGui.DialogApp.Dialog.NormalDialog.Visible then
														pcall(function()
															getconnections(Player.PlayerGui.DialogApp.Dialog.NormalDialog.Buttons.ButtonTemplate.MouseButton1Down)[1]:Fire()
															wait(.1)
															getconnections(Player.PlayerGui.DialogApp.Dialog.NormalDialog.Buttons.ButtonTemplate.MouseButton1Up)[1]:Fire()
															getconnections(Player.PlayerGui.DialogApp.Dialog.NormalDialog.Buttons.ButtonTemplate.MouseButton1Click)[1].Function()
														end)
													end
												end
												
											end
										end
									end
								end
							end
						end
						--]]
					end)
					if err then
						con_log(err, "ERROR hatching:")
					end
				end
				
				
				
				
				---------------------------------
				-- get food
				---[==[
				local f_num, w_num = 0, 0
				for _,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].inventory.food do
					if v.id == "pizza_shop_cheese" then
						f_num = f_num + 1
					end
					if v.id == "soda_fountain_water" or v.id == "soda_fountain_soda" then
						w_num = w_num + 1
					end
				end
				if f_num < 5 or w_num < 2 then
					if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].char_wrapper_raw.location.destination_id ~= "PizzaShop" then
						GoToStore("PizzaShop")
						--Workspace.Pstone.CFrame = Player.Character.HumanoidRootPart.CFrame * CFrame.new(Vector3.new(0,-3,0))
					end
					if f_num < 5 then
						local fur = "f-12"
						for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].house_interior.furniture do
							if v.id == "pizza_shop_ingredients_crates" then
								fur = i
								break
							end
						end
						for i=1,30 do
							ReplicatedStorage.API["HousingAPI/ActivateInteriorFurniture"]:InvokeServer(fur, "Cheese", {}, Player.Character)
						end
					end
					if w_num < 2 then
						for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].house_interior.furniture do
							if v.id == "pizza_shop_coaster" and not v.drink then
								ReplicatedStorage.API["HousingAPI/ActivateInteriorFurniture"]:InvokeServer("f-54", "Water", {}, Player.Character)
								wait(.05)
								ReplicatedStorage.API["HousingAPI/ActivateInteriorFurniture"]:InvokeServer(i, "UseBlock", {}, Player.Character)
								wait(.05)
							end
						end
						ReplicatedStorage.API["HousingAPI/ActivateInteriorFurniture"]:InvokeServer("f-54", "Water", {}, Player.Character)
						wait(.5)
						for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].house_interior.furniture do
							if v.id == "pizza_shop_coaster" and v.drink then
								ReplicatedStorage.API["HousingAPI/ActivateInteriorFurniture"]:InvokeServer(i, "UseBlock", {}, Player.Character)
								wait()
							end
						end
					end
				end
				--]==]
				---------------------------------
				-- red tasks --
				for _,v in red_task do
					if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.baby_ailments[v] then
						if not event_flag then
							red_work(v)
						end
						break
					elseif require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID] then
						if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID][v] then
							if not event_flag then
								red_work(v)
							end
							break
						end
					end
				end
				---------------------------------
				-- blue task
				for _,v_b in bb_task do
					local done, done2
					if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.baby_ailments[v_b] then
						spawn(function()
							done,_=pcall(function()
								work[v_b](true)
								wait(1)
							end)
						end)
						
						spawn(function()
							done2,_=pcall(function()
								for _,v in eat_task do
									if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.baby_ailments[v] then
										work[v](true)
										wait(1)
									end
								end
							end)
						end)
						if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID] then
							for _,v in care_task do
								if v ~= v_b then
									if v == "play" and done2 == nil then
									else
										if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID][v] then
											work[v](false)
											wait(1)
										end
									end
								end
								if done ~= nil then
									break
								end
							end
						end
						while done == nil or done2 == nil do
							wait()
						end
						--break
					end
				end
				
				local done2
				spawn(function()
					done2,_=pcall(function()
						for _,v in eat_task do
							if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.baby_ailments[v] then
								work[v](true)
								wait(1)
							end
						end
					end)
				end)
				wait()
				local chk
				for _,v in care_task do
					--con_log(v, tostring(done2))----
					if v == "play" and done2 == nil then
					else
						if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID] then
							if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID][v] then
								work[v](false)
								wait(1)
								chk = true
								if done2 ~= nil then
									break
								end
							end
						end
					end
				end
				for _,v in wend_task do
					if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID] then
						if require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID][v] then
							if chk then
								break
							end
							if done2 ~= nil then
								if not event_flag then
									work[v]()
									wait(1)
								end
								if v ~= "pet_me" then
									break
								end
							elseif v == "walk" and not require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID][v].components.preference then
								if not event_flag then
									work[v]()
									wait(1)
								end
								break
							--elseif v == "ride" and require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID][v].components.preference then
								--if not require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID][v].components.preference.item_kind:find("stroller") then
									--work[v]()
									--break
								--end
							end
						end
					end
				end
				while done2 == nil do
					wait()
				end
				
				---------------------------------
				-- mystery task
				---[===[
				
				local str_ail
				local nomstr
				local d = {}
				for i,v in require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].ailments_manager.ailments[PetID] or {} do
					local tmp1, tmp2 = false, false
					if i:find("mystery") then
						tmp2 = true
						for ii,vv in v.components.mystery.components do
							if vv.preference then
								tmp1 = true
							end
						end
					else
						if table.find(street_task, i) then
							str_ail = true
						else
							if table.find(red_task, i) or table.find(wend_task, i) or table.find(care_task, i) then
								nomstr = true
							end
						end
					end 
					wait(.3)
					table.insert(d, {0, os.time()-v.created_timestamp, i, tmp1, tmp2})
					table.sort(d, function (a, b) return a[2] > b[2] end)
				end
				for i,v in d do
					d[i][1] = i
				end
				local c1, c2 = {}, {}
				for i,v in d do
					if v[4] and v[5] then
						table.insert(c1, v)		--preferences
					elseif v[5] then
						table.insert(c2, v)		--mysterys no pref
					end
				end
				d={}
				for i,v in c1 do
					table.insert(d, v)
				end
				for i,v in c2 do
					table.insert(d, v)
				end
				--local a=""
				--for i,v in d do
					--a=a..tostring(v[1]).." "..tostring(v[2]).." "..tostring(v[3]).." "..tostring(v[4]).." "..tostring(v[5]).."\n"
				--end
				--setclipboard(a)
				if redstr_flag and str_ail then
					for i,v in d do
						local tmp = {}
						if v[3] == redstr_flag then
							table.insert(tmp, {v[1], v[2], v[3], v[4], v[5]})
							redstr_flag = mystery(tmp)
							--redstr_flag = mystery({{v}})
							nomstr = true
							break
						end
					end
				end
				--con_log(nomstr, #c1)
				if #d > 0 and not nomstr then
					if #d == 1 and redstr_flag then
					else
						redstr_flag = mystery(d)
					end
				end
				
				--]===]

				
				
				---------------------------------
				
				if not event_flag then
					busy = false
				end
			elseif busy then
				--con_log(event_flag,"busy")
			end
			
			
			
			
			if #Bankers > 0 then
				
				local flag = false
				for _,Banker in next, Bankers do
					local _,err=pcall(function()
					
						if tonumber(Options["Input"..Banker].Value) <= 0 then
						else
							local ready
							local check,ans=pcall(function()
								local tmp = Players[Banker]
							end)if ans then con_log(ans, Banker) end
							
							local _,ans=pcall(function()
							if check and require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Banker].char_wrapper_raw.location then
								ready = true
							end
							end)if ans then con_log(ans, "error location "..tostring(Banker)) end
							
							if not ready then
								kassa[Banker] = nil
							else
							--if ready then
								--local _,err=pcall(function()
									if os.time() - timer_b < 120 then
									elseif require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].money > 1600 then
										local tmp_cash = tonumber(Options["Input"..Banker].Value)
										for i = 1,3 do
											
											if not kassa[Banker] then
												local home = Workspace.HouseInteriors.blueprint:FindFirstChildWhichIsA("Model")
												if not home or home.Name ~= Banker then
													ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(PetID)
													GoToHome(Banker)
													wait(2)
													Pets()
												end
												local fur = Furniture(cashbox)
												if not fur then
													con_log("cashregister not found!")
													break
												end
												kassa[Banker] = fur
												ReplicatedStorage.API["ToolAPI/Unequip"]:InvokeServer(PetID)
												GoToHome()
												--ReplicatedStorage.API["TeamAPI/ChooseTeam"]:InvokeServer("Babies", {},true)
												wait(2)
												Pets()
											end
											
											local cash_chips = 50
											local bal
											
											tmp_cash = tmp_cash - cash_chips
											if tmp_cash < 0 then
												cash_chips = tmp_cash + cash_chips
												tmp_cash = 0
											end
											local tmp_time = os.time()
											bal = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].money
											ReplicatedStorage.API["HousingAPI/ActivateFurniture"]:InvokeServer(Players[Banker], kassa[Banker], "UseBlock", cash_chips, Player.Character)
											bal = bal - require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[Player.Name].money
											con_log(tostring(bal).." ("..tostring(os.time()-tmp_time).."sec.)", tostring(Banker).." send:")
											
											--if bal > 10 then
												if #tmp_Bankers > 0 then
													local tmp = {}
													for _,v in next,tmp_Bankers do
														if v[1] == Banker then
														else
															table.insert(tmp, {v[1], v[2]})
														end
													end
													tmp_Bankers = tmp
												end
												table.insert(tmp_Bankers, {Banker, tmp_cash})
												
												for num = 1, #info_bank do
													local tmp = string.split(info_bank[num], " ")
													if tmp[1] == tostring(Banker) then
														if tonumber(tmp[3]) == 0 then
															table.insert(info_bank, 1, tostring(Banker).." "..tostring(cash_chips).." "..tostring(tmp_cash))
															break
														end
														info_bank[num] = tostring(Banker).." "..tostring(tonumber(tmp[2])+cash_chips).." "..tostring(tmp_cash)
														break
													end
													if num == #info_bank then
														table.insert(info_bank, 1, tostring(Banker).." "..tostring(cash_chips).." "..tostring(tmp_cash))
													end
												end
												if #info_bank == 0 then
													table.insert(info_bank, 1, tostring(Banker).." "..tostring(cash_chips).." "..tostring(tmp_cash))
												end
												--if info_bank[Banker] then
													--info_bank[Banker] = info_bank[Banker] + cash_chips
												--else
													--info_bank[Banker] = cash_chips
												--end
											--end
											flag = true
											if tmp_cash <= 0 then
												break
											end
											
											--Options["Input"..Banker]:SetValue(tostring(tmp_cash))
											--Options["Input"..Banker].Text = tostring(tmp_cash)
											--Options["Input"..Banker]:SetValue(456)
											--circle = circle + 1
										end
									else
										con_log("insufficient funds!")
									end
								
								--end)if err then con_log(err, "ERROR trans cash:")end
							end
						end
					end)if err then con_log(err, "ERROR trans cash:")end
				end  --Bankers				
				
				if os.time() - timer_b > 120 then
					timer_b = 0
				end
				if timer_b == 0 and flag then
					timer_b = os.time()
					
					local _,err=pcall(function()
						if #info_bank > 25 then
							table.remove(info_bank)
							table.remove(info_bank)
						end
						local tmp = ""
						for iiii,vvvv in info_bank do
							--tmp = tmp..tostring(iiii)..": "..tostring(vvvv).."\n"
							if vvvv ~= "" then
								tmp = tmp..vvvv.."\n"
							end
						end
						writefile("transfer.txt", tmp)
					end)if err then con_log(err, "ERROR write file:")end
				end
			end	
			if #Bankers > 0 then
				con_log(os.time() - timer_b, "bankers timer:")
			end
			
		
		end)if err then con_log(err); busy = false end
	
	end
	con_log("thread script stopped!")
end)()

wait(5)
Options.Farm:SetValue(true)
wait(1)
--Options.Event1:SetValue(true)
--Options.Event2:SetValue(true)
Options.Trans:SetValue(true)


--]]
--con_log("done")























